# Parytet funkcji: PWA (web) vs aplikacja Android (Kotlin)

Ta tabela śledzi, które wymagania funkcjonalne (`Functional requirements/FR-*.md`,
opisujące zachowanie wersji webowej) mają już odpowiednik w natywnej aplikacji
Android (`android/`). Aktualizowana przy każdej zmianie po obu stronach — jeśli
coś tu jest oznaczone ✅, a w Kotlinie realnie nie działa (bo np. nie zdążyłeś
jeszcze sprawdzić w Android Studio), popraw status na ⏳ do potwierdzenia.

**Legenda „Status w Android":** ✅ zaimplementowane i (na ile się dało) zweryfikowane · ⏳ zaimplementowane, czeka na sprawdzenie w Android Studio · ⬜ jeszcze nie rozpoczęte · N/D nie dotyczy natywnej aplikacji (mechanizm specyficzny dla PWA/Service Workera)

**Legenda „Status w web":** ✅ zaimplementowane w `index.html` (jeśli nieoczywiste — z zastrzeżeniem w nawiasie, np. wymaganymi regułami Firestore) · ⏳ częściowe/świadomie niepełne w web samym w sobie · 🔗 scalone z innym FR, funkcja nadal istnieje pod innym numerem · ❌ wyłączone/wycofane w web (na razie nie dotyczy — kolumna istnieje żeby to było łatwe do zaznaczenia w przyszłości). Źródło: pole `**Status:**` w nagłówku każdego `Functional requirements/FR-N.md` — jeśli je tam zmienisz (np. świadomie wyłączysz jakąś funkcję w web), zaktualizuj też tę kolumnę, żeby oba pliki się zgadzały.

| FR | Wymaganie | Status w web | Status w Android |
|---|---|---|---|
| FR-1 | Baza przepisów podzielona na 5 kategorii posiłków | ✅ | ✅ brak znanego pozostałego braku funkcjonalnego (patrz uwagi niżej) |
| FR-2 | Wyszukiwanie i filtrowanie przepisów | ✅ | ⏳ wszystkie przełączniki + próg oceny zaimplementowane 2026-08-11, jeden świadomy wyjątek (patrz uwagi niżej) |
| FR-3 | Karta przepisu — widok skrócony i rozwinięty | ✅ | ⏳ dwa prawdziwe braki naprawione 2026-08-11 (pojedyncze rozwinięcie + auto-wyśrodkowanie); web'owe dwuetapowe otwieranie (v4, 2026-08-11) jeszcze NIE przeniesione (patrz uwagi niżej) |
| FR-4 | Miniatura przepisu jako emoji głównego składnika | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-5 | Przycisk powrotu do góry listy przepisów | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-6 | Profil użytkownika i wyliczanie zapotrzebowania kalorycznego | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-7 | Podział dziennego celu kalorycznego na 5 posiłków | ✅ | ✅ zaimplementowane jako część FR-6 (ProfileCalculations.calcTargets) |
| FR-8 | Filtr bez glutenu / bez laktozy | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-9 | Przełącznik rygoru niskiego indeksu glikemicznego | ✅ | ✅ kara za wysoki ładunek glikemiczny w RecipeMatching.matchScore, testy JUnit (patrz uwagi niżej) |
| FR-10 | Docelowe proporcje makroskładników zależne od celu | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-11 | Wynik dopasowania przepisu do profilu (🎯) | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-12 | Modal wyjaśniający wyliczenia makro/IG/ŁG | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze (bez mikroskładników, patrz uwagi) |
| FR-13 | Piąta kategoria posiłku: Deser/Przekąska | ✅ | ✅ wszystkie 3 kryteria akceptacji spełnione (patrz uwagi niżej) |
| FR-14 | Skalowanie rozmiaru interfejsu (UI scale) | ✅ | ✅ zaimplementowane, trwały zapis dodany 2026-08-10 (patrz uwagi niżej) |
| FR-15 | Oznaczanie dania jako ugotowane, z historią i ocenami | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-16 | Sprawdzenie stanu spiżarni dla konkretnego przepisu | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-17 | Ocena dania po ugotowaniu (gwiazdki) | ⚠️ nieaktualne, scalone w FR-84 | ⚠️ nieaktualne, scalone w FR-84 — historia gotowania jest teraz czystym logiem dat, patrz notatka FR-84 niżej |
| FR-18 | Planer tygodniowy z 5 slotami posiłków dziennie | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-19 | Wybór innego slotu posiłkowego z poziomu karty przepisu | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-20 | Skalowanie wielkości porcji w planerze | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-21 | Losowe generowanie planu — cały tydzień lub pojedynczy dzień | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-22 | Czyszczenie planu — cały tydzień lub pojedynczy dzień | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze (per dzień — patrz uwagi) |
| FR-23 | „Ugotuj na 2 dni” — planowanie resztek po zwiększeniu porcji | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-24 | Proaktywna podpowiedź gotowania na kolejny dzień | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-25 | Budowanie listy zakupów ze składników przepisów | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-26 | Odhaczanie, udostępnianie i czyszczenie listy zakupów | ✅ | ✅ udostępnianie + "Wyczyść całą listę" dodane 2026-08-10, zweryfikowane na emulatorze (patrz uwagi niżej) |
| FR-27 | Dodanie składników z całego tygodnia z Planera | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-28 | Śledzenie stanu spiżarni w kafelkach pogrupowanych kategoriami | ✅ | ✅ przebudowane od zera 2026-08-10 na pełną siatkę kafelków (patrz uwagi niżej) — web'owa poprawka wydajności z 2026-08-11 (v3) nie dotyczy Androida, ten kod nigdy nie odświeżał niewidocznych zakładek; „🗑️ Wyczyść całą spiżarnię" (v4) dodane na obu platformach w tej samej turze |
| FR-29 | Odmiana gramatyczna nazw produktów w spiżarni | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-30 | Zmiana kategorii i usuwanie śledzenia kafelka spiżarni | ✅ | ✅ przebudowane 2026-08-10 razem z FR-28 (patrz uwagi niżej) |
| FR-31 | Skanowanie kodu kreskowego produktu | ❌ wyłączone (przycisk ukryty, kod zostaje) | N/D — celowo nierozpoczęte, funkcja wyłączona na obu platformach na życzenie użytkownika (2026-08-10), nie będzie rozwijana na ten moment |
| FR-32 | Podpowiedź „🏺 masz w spiżarni” i „Pomysł na danie z ulubionych składników” | ✅ | ✅ w pełni zweryfikowane na emulatorze 2026-08-10, w tym ☆→★ i realnie wygenerowany pomysł (patrz uwagi niżej) |
| FR-33 | Globalny przycisk szybkiego dodania przekąski/dania z każdego miejsca | ✅ | ✅ pływający przycisk na Postępie dodany 2026-08-10, zweryfikowany na emulatorze (patrz uwagi niżej) |
| FR-34 | Automatyczne szacowanie kalorii przekąski z bazy 336 produktów | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze (patrz uwagi) |
| FR-35 | Emotikonki przy rozpoznanych składnikach/przekąskach | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze (patrz uwagi) |
| FR-36 | Dzienny podwójny pierścień kalorii/nawodnienia w nagłówku ze zjadanymi posiłkami | ✅ | ✅ przeprojektowane na podwójny pierścień 2026-08-11, zweryfikowane na emulatorze w obu motywach (patrz uwagi) |
| FR-37 | Śledzenie nawodnienia — pełny widok, kompaktowy pasek w nagłówku i wewnętrzny łuk pierścienia | ✅ | ✅ zaimplementowane 2026-08-10 (pełny widok w nowej zakładce Postęp, dzieli WaterViewModel z paskiem w nagłówku); kubeczek zamiast kropelki + trzecie zsynchronizowane miejsce (wewnętrzny łuk pierścienia) dodane 2026-08-11, zweryfikowane na emulatorze |
| FR-38 | Powiadomienia z szybkimi akcjami do liczenia wody | ✅ | ✅ zaimplementowane 2026-08-11, zweryfikowane na emulatorze (patrz uwagi niżej) |
| FR-39 | Cykliczne przypomnienie o piciu wody | ✅ | ✅ zaimplementowane 2026-08-11, zweryfikowane na emulatorze (patrz uwagi niżej) |
| FR-40 | Śledzenie wagi z wykresem | ✅ | ✅ zaimplementowane 2026-08-10, zweryfikowane na emulatorze (patrz uwagi niżej) |
| FR-41 | Historia kalorii z bilansem tygodniowym | ✅ | ⏳ wykres + bilans zaimplementowane 2026-08-10, historia dopiero zaczyna się gromadzić od tej daty (patrz uwagi niżej) |
| FR-42 | Serie (streaks) i historia aktywności | ✅ | ✅ serie i surowy log aktywności zaimplementowane i zweryfikowane na żywo 2026-08-10 (patrz uwagi niżej) |
| FR-43 | Pasek filtrów i kategorii przyklejony pod nagłówkiem | ✅ (v3, 2026-08-11: chowa się razem z nagłówkiem) | ✅ port "chowa się razem z nagłówkiem" dokończony 2026-08-11 tego samego dnia (`RecipeListScreen`'s nowy parametr `headerExpanded`, patrz uwagi) |
| FR-44 | Automatyczne chowanie/pokazywanie nagłówka na przewijanie (tylko Przepisy) | ✅ (v2, 2026-08-11: tylko blisko góry) | ✅ port "tylko blisko góry" dokończony 2026-08-11 tego samego dnia (`HeaderScrollBehavior.isNearTop`, `scrolledDown`/`scrolledUp` usunięte jako martwy kod) |
| FR-45 | Ręczne zwijanie/rozwijanie nagłówka ma pierwszeństwo nad automatyką | ✅ (v4, 2026-08-11: też rozwinięcie) | ✅ port "też rozwinięcie zamraża automatykę" dokończony 2026-08-11 tego samego dnia |
| FR-46 | Zabezpieczenie przed przypadkowym zamknięciem aplikacji (Android „Wstecz”) | ✅ | ✅ już spełnione przez istniejącą architekturę, zweryfikowane na emulatorze (patrz uwagi) |
| FR-47 | Brak migotania (FOUC) domyślnych danych profilu przy odświeżeniu | ✅ | ✅ strukturalnie niemożliwe do naruszenia w tej architekturze (patrz uwagi) |
| FR-48 | Wybór motywu kolorystycznego aplikacji | ✅ | ✅ oba pierwotne blokery usunięte (trwały zapis 2026-08-10, pasek statusu 2026-08-11, patrz uwagi niżej) |
| FR-49 | Motyw „Polaroid” z kartami w stylu odbitek natychmiastowych | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze (patrz uwagi FR-48) |
| FR-50 | Redukcja animacji (prefers-reduced-motion) | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-51 | Instalowalna aplikacja PWA z ikoną i manifestem | ✅ | N/D — mechanizm specyficzny dla PWA (instalacja/Service Worker/cache), nie dotyczy natywnej appki |
| FR-52 | Cache offline przez Service Worker ze strategią stale-while-revalidate | ✅ | N/D — mechanizm specyficzny dla PWA (instalacja/Service Worker/cache), nie dotyczy natywnej appki |
| FR-53 | Ręczne wymuszenie aktualizacji i diagnostyka powiadomień | ✅ | N/D — mechanizm specyficzny dla PWA (instalacja/Service Worker/cache), nie dotyczy natywnej appki |
| FR-54 | Kopie zapasowe wersji plików aplikacji w repozytorium | ✅ | N/D — mechanizm specyficzny dla PWA (instalacja/Service Worker/cache), nie dotyczy natywnej appki |
| FR-55 | Ocenianie przepisów przesunięciem karty (lubię / nie lubię) | ✅ (od FR-84: skrót do oceny gwiazdkowej) | ✅ zaimplementowane, zaktualizowane pod FR-84 2026-08-11, zweryfikowane na emulatorze |
| FR-56 | Duży, balonowy napis podczas oceniania przesunięciem | ✅ | ✅ zweryfikowane na emulatorze — napis „❤️ Podoba się to dla mnie!"/„Nie podoba się to dla mnie!" widoczny w trakcie przesunięcia (treść ujednolicona z web 2026-08-11, patrz uwagi) |
| FR-57 | Trwałe oznaczenie oceny i ranking sort | ⚠️ zmienione przez FR-84 (plakietka ★N, przełącznik sortowania usunięty) | ✅ zaimplementowane pod FR-84 2026-08-11, zweryfikowane na emulatorze (plakietka „★5" + zielona obwódka po ocenie) |
| FR-58 | Dodawanie składników z konkretnego dnia na liście zakupów | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-59 | Wyśrodkowane okienka modalne, na pełną dostępną szerokość | ✅ | ✅ w większości już spełnione przez architekturę Compose, dociągnięte i zweryfikowane na emulatorze (patrz uwagi) |
| FR-60 | Warunkowe wyświetlanie „Złotych zasad przy Hashimoto i insulinooporności” | ✅ | ✅ zaimplementowane 2026-08-10, zweryfikowane na emulatorze |
| FR-61 | Wybór stylu oceniania kart przesunięciem w Ustawieniach | ✅ | ✅ gest przesunięcia (styl domyślny „balloon") zweryfikowany na emulatorze 2026-08-10 (patrz uwagi FR-56) |
| FR-62 | Mini kalendarzyk bieżącego tygodnia na liście zakupów | 🔗 połączone z FR-58 | ✅ scalone z FR-58 w jeden widżet (patrz uwagi FR-58/FR-62) — to samo źródło prawdy co web po rewizji v2 |
| FR-63 | Motywy „Fluent” i „Kafelki” inspirowane Windows 11 / Metro | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze (patrz uwagi FR-48) |
| FR-64 | Orientacyjne wartości mikroskładników (wapń, wit. D, B12) w okienku wyliczeń | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze |
| FR-65 | Własna, opcjonalna nazwa użytkownika w aplikacji | ✅ | ✅ oba pierwotne blokery usunięte (patrz uwagi niżej) |
| FR-66 | Dodawanie własnych przepisów przez użytkownika | ✅ | ✅ auto-obliczanie makro ze składników dodane 2026-08-11, zweryfikowane na emulatorze (patrz uwagi niżej) |
| FR-67 | Ocena gwiazdkowa i komentarz przy przepisie | ✅ (od FR-84: jedyny mechanizm oceniania) | ⏳ zaimplementowane 2026-08-10, lokalnie (bez wielo-użytkownikowego wątku komentarzy z FR-77, patrz uwagi niżej); stało się jedynym mechanizmem oceniania 2026-08-11, patrz notatka FR-84 |
| FR-84 | Scalenie oceniania przepisu w jeden mechanizm | ✅ | ✅ zaimplementowane i zweryfikowane na żywo na emulatorze 2026-08-11 (patrz uwagi niżej) |
| FR-68 | Ustawienia gospodarstwa domowego i przepisów społeczności (stan przejściowy) | ⏳ świadomie częściowe (przejściowy stan przed `households/*`) | ✅ przełącznik „🌍 Przepisy społeczności” + „👥 Przeglądaj użytkowników” dodane 2026-08-11 (patrz uwagi FR-76 niżej) |
| FR-69 | Logowanie w chmurze (anonimowe, Google, e-mail i hasło) | ⏳ logowanie ✅, synchronizacja danych jeszcze nie (patrz FR-73) | ⏳ anonimowe + e-mail/hasło + Google zaimplementowane; SHA-1 zarejestrowany przez użytkownika 2026-08-11, kompilacja ponownie potwierdzona, rzeczywiste logowanie Google na urządzeniu wciąż czeka na ręczne potwierdzenie (patrz uwagi niżej) |
| FR-70 | Licznik nawodnienia w nagłówku — pojedyncze klikalne kropelki | ✅ | ✅ oba pierwotne blokery usunięte 2026-08-10 (patrz uwagi niżej) |
| FR-71 | Zakładki w Ustawieniach — Konto, Wygląd, Przypomnienia, Ulubione | ✅ | ✅ wszystkie 4 zakładki mają teraz realną zawartość, zweryfikowane na emulatorze (patrz uwagi niżej) |
| FR-72 | Wymuszenie ustawienia profilu przy pierwszym uruchomieniu | ✅ | ✅ zaimplementowane i ręcznie zweryfikowane na emulatorze (patrz uwagi) |
| FR-73 | Synchronizacja danych osobistych w chmurze między urządzeniami | ✅ | ⏳ pantry naprawione + zakres znacznie rozszerzony 2026-08-10, w tym naprawiony błąd niszczący historię wielodniową web'a (patrz uwagi niżej), zweryfikowane budowaniem/testami, nie na dwóch prawdziwych urządzeniach jednocześnie |
| FR-74 | Wspólna zakładka „Śniadania” na liście przepisów, osobne sloty w Planerze | ✅ | ✅ zweryfikowane na emulatorze 2026-08-10 — pigułka „🔍 Śniadania” na Przepisach, 5 osobnych slotów (Śniadanie/II Śniadanie/Obiad/Kolacja/Deser) w Planerze |
| FR-75 | Widok kafelkowy listy zakupów z brakującymi ilościami | ✅ | ✅ zweryfikowane na emulatorze 2026-08-10 — siatka kafelków z emoji, nazwą i czerwoną plakietką „−ilość” renderuje się poprawnie (patrz uwagi) |
| FR-76 | Przepisy społeczności oraz przeglądana lista użytkowników i profili | ✅ (wymaga reguł Firestore w konsoli; v2 2026-08-11: limit czasu na zapytania) | ⏳ zaimplementowane 2026-08-11, wymaga wklejenia reguł Firestore + weryfikacji dwoma kontami (patrz uwagi niżej) — Android nie miał web'owego "wisi na Wczytywanie…" błędu (`UserListViewModel`/`UserProfileViewModel` miały stan `Unavailable` od początku) |
| FR-77 | Komentarze wielu użytkowników pod przepisem, z paginacją | ✅ (wymaga reguł Firestore w konsoli) | ⏳ zaimplementowane 2026-08-11, wymaga wklejenia reguł Firestore + weryfikacji dwoma kontami (patrz uwagi FR-76 niżej) |
| FR-78 | Pełna synchronizacja stanu z prawdziwym scalaniem zmian (3-way merge) | ✅ (wymaga reguł Firestore w konsoli) | ⏳ świadomie UPROSZCZONY port 2026-08-11 -- last-write-wins PER POLE zamiast pełnego 3-way merge z oknem konfliktu, na wyraźną prośbę użytkownika (patrz nowa notatka niżej) |
| FR-79 | Wylogowanie z urządzenia | ✅ | ✅ zaimplementowane 2026-08-10, oba okienka potwierdzenia + wyczyszczenie danych lokalnych (patrz uwagi niżej) |
| FR-80 | Dzień tygodnia przy składniku na liście zakupów | ✅ | ✅ zaimplementowane 2026-08-11 tego samego dnia co web, testy JUnit (patrz uwagi niżej) |
| FR-81 | Propozycja przeliczenia planu i listy zakupów po zapisaniu profilu | ✅ | ✅ zaimplementowane 2026-08-11 tego samego dnia co web, zweryfikowane na żywo na emulatorze (patrz uwagi niżej) |
| FR-82 | Widoczna wersja aplikacji w Ustawieniach | ✅ | ✅ Android miał to już od wcześniej (`AppUpdateCard`'s „Zainstalowana wersja: X”) — FR dopisany 2026-08-11 tylko formalnie, bez zmian w Kotlinie |
| FR-83 | Edycja wcześniej wpisanej wagi i historii kalorii | ✅ (waga) / ✅ (historia kalorii) | ✅ waga (edycja+usuwanie inline, zweryfikowane na emulatorze) / ⬜ historia kalorii świadomie odłożona — wymaga przebudowy `EatenViewModel`/`EatenEntry` z per-kategoria na per-data (patrz uwagi FR-83.md i notatka niżej) |

## Uwagi do częściowych wpisów

- **Runda 2026-08-11 (druga połowa dnia) — 6 drobnych/średnich poprawek na wyraźną prośbę użytkownika, wszystkie tego samego dnia**: (1) Dotknięcie tytułu przepisu w Kotlinie otwiera teraz wyszukiwanie Google (`"${recipe.name} przepis"`), port DOTĄD NIEUDOKUMENTOWANEJ (brak osobnego FR) web'owej funkcji `.recipe-title`'s click handler — nie ma osobnego wiersza w tabeli powyżej, bo web sam nigdy nie miał na to FR-a; uznane za lukę w dokumentacji web'a, nie w porcie. (2) FR-44/v2 + FR-45/v4 + FR-43/v3: dokończony port "header + pasek filtrów tylko blisko góry" (patrz wiersze FR-43/44/45 powyżej). (3) **Naprawiony realny błąd nawigacji**: w Ustawieniach, jeśli jakiekolwiek pole tekstowe miało fokus (klawiatura/pasek sugestii widoczny), dotknięcie ikony w dolnym pasku nawigacji (np. Spiżarnia) było CAŁKOWICIE ignorowane — dopiero systemowy przycisk Wstecz (który przy okazji czyści fokus) odblokowywał nawigację. Zreprodukowane i potwierdzone bezpośrednio na emulatorze (`uiautomator dump` przed/po każdym dotknięciu). Naprawione: `focusManager.clearFocus()` + `keyboardController?.hide()` wołane W TEJ SAMEJ akcji `onClick`, co nawigacja (i w przycisku ustawień, i w każdym przycisku dolnego paska) — dokładnie ten sam pojedynczy tap teraz i czyści fokus, i nawiguje, zamiast wymagać dwóch osobnych akcji. (4) **Naprawiony realny błąd aktualizacji** — patrz osobna notatka niżej. (5) FR-80 (dzień tygodnia na liście zakupów) i (6) FR-81 (propozycja przeliczenia planu po zapisaniu profilu) — obie NOWE funkcje zaimplementowane jednocześnie na obu platformach w tej turze, patrz ich własne wiersze/notatki. Wszystko zweryfikowane bezpośrednio na emulatorze na żywo (nie tylko kompilacją) — w tym pełny przebieg FR-81 (dwa okienka z rzędu) i wynikowa lista zakupów z poprawnymi etykietami dni z FR-80.

- **FR-84 — scalenie oceniania w jeden mechanizm (2026-08-11, piąta runda tego dnia)**: na wyraźną prośbę użytkownika ("scal w jedno system gwiazdek, oceny po zrobieniu dania oraz ocene i komentarz ktory mozna dodać pod przepisem, to jedno i to samo"), trzy dotychczas osobne systemy oceniania (przesunięcie karty ❤️/👎, gwiazdka za każde „✅ Zrobione", ocena+komentarz z FR-67) stały się JEDNYM — samą oceną z FR-67 (`RecipeReview`/`recipeReviews`). Android: `RecipeViewModel.setRatingQuick(recipeId, stars)` — nowa funkcja wołana zarówno przez przesunięcie karty (prawo=5★, lewo=1★, zachowuje istniejący komentarz), jak i przez nowy przycisk „⭐ Oceń to danie"/„⭐ Twoja ocena: X/5 (zmień)" w `CookHistoryDialog` (który stał się czystym logiem dat — usunięto `onSetRating`/`StarRatingRow` per-wpisowy). Plakietka w rogu karty pokazuje „★N" (zamiast dawnego 👍/👎) i po dotknięciu otwiera okienko oceny zamiast je kasować. Przełącznik sortowania „❤️ Ranking" (`RecipeRatingOperations.sortByRating`) usunięty z paska filtrów jako redundantny z „🏆 Ocena". Migracja: `RecipeViewModel.replaceRatings` (wołana przy każdym wczytaniu lokalnym i pull z chmury) jednorazowo konwertuje stare wpisy `RecipeRating` bez odpowiadającej im recenzji na recenzję (LIKE→5★, DISLIKE→1★), nigdy nie nadpisując istniejącej prawdziwej recenzji. Świadomie NIE usunięto `RecipeRatingOperations`/`CookHistoryOperations.setRating` z modułu `logic` (razem z ich testami JUnit) — zostają jako nieużywany, ale nieszkodliwy kod, żeby nie poszerzać zakresu zmiany o usuwanie testów w tej samej turze. Zweryfikowane bezpośrednio na emulatorze: ocena 5★ przez okienko poprawnie pokazała plakietkę „★5" z zieloną obwódką karty, historia gotowania pokazuje czysty log dat bez własnych gwiazdek, przycisk „⭐ Oceń to danie" poprawnie otwiera to samo okienko co plakietka. Zero crashy. `versionCode`/`versionName` bump w `app/build.gradle.kts`.

- **Naprawiony realny błąd utraty danych profilu (2026-08-11, czwarta runda tego dnia)**: użytkownik zgłosił "w kotlin nie zapamiętuje mi w ustawieniach że jestem mężczyzną, przełącza mi na kobietę... cel zmieniałem a teraz widzę znów z defaultu wstawił" — realny, potwierdzony błąd, najprawdopodobniej wprowadzony/odsłonięty przez zmianę z drugiej rundy tego dnia (FR-78 dirty-field-tracking). Przyczyna: `lastKnownFields` (mapa "ostatnia wartość, co do której to urządzenie i Firestore się zgodziły") żyła WYŁĄCZNIE w pamięci (`remember`), resetując się do pustej przy KAŻDYM restarcie aplikacji. Sekwencja gubiąca dane: użytkownik edytuje profil, zamyka aplikację ZANIM 1,5-sekundowy debounce zdąży wypchnąć zmianę do chmury; po ponownym uruchomieniu edycja poprawnie wraca z lokalnego dysku, ale `lastKnownFields` startuje puste, więc PIERWSZY odebrany snapshot z Firestore (wciąż ze STARĄ wartością) wyglądał jak "coś nowego" i cicho nadpisywał świeżą lokalną edycję. Naprawione: nowy `CloudSyncBaselineStore` (`android/app/.../data/CloudSyncBaselineStore.kt`) zapisuje `lastKnownFields` na dysk (per uid, reużywa `LocalStateStore`'s JSON I/O pod inną nazwą pliku), wczytywany PRZED dopuszczeniem nasłuchiwacza Firestore do podłączenia się (`baselineLoaded` blokuje `DisposableEffect`) — więc nawet PIERWSZY snapshot po zimnym starcie poprawnie odróżnia "to pasuje do tego, co już wiedziałem" (ignoruj) od "to naprawdę inne niż ostatnio wiedziałem" (zastosuj — dotyczy też prawdziwie nowego urządzenia/logowania, gdzie baseline jest pusty celowo). Przy okazji usunięto `PushedSnapshot`/`lastPushed`/`suppressNextPush` jako w pełni zastąpione przez trwały `lastKnownFields` — mniej kodu, jeden mechanizm zamiast dwóch częściowo nakładających się. Zweryfikowane bezpośrednio na emulatorze: zmiana płci na Mężczyzna → `adb shell am force-stop` ~900ms później (po lokalnym zapisie na dysk, przed wypchnięciem do chmury) → ponowne uruchomienie → wartość poprawnie przetrwała I pozostała stabilna po pełnej synchronizacji z chmurą (sprawdzone dwoma pełnymi cyklami restart+odczekanie), zero crashy w logcat.

- **FR-83 (2026-08-11, trzecia runda tego dnia)**: waga (edycja/usuwanie wpisu) zaimplementowana i zweryfikowana na obu platformach — Android: `WeightOperations.editWeight`/`removeWeight` (+ testy JUnit) i `WeightCard`'s nowe inline wiersze z ✏️/🗑, na żywo na emulatorze (edycja wpisu z 07.08.2026 z 73 na inną wartość zadziałała poprawnie inline, bez osobnego okna). Historia kalorii (nawigacja dat w karcie „co zjadłam”) zaimplementowana TYLKO na web — świadomie NIE przeniesiona na Androida w tej samej turze, bo wymagałaby zmiany kształtu danych (`EatenViewModel._entries: Map<String, EatenEntry>` trzyma WYŁĄCZNIE dzisiejszy stan per kategoria, `_kcalHistory` to tylko pochodna suma dzienna bez zapisanych pojedynczych zaznaczeń dla przeszłych dni), nie tylko UI jak przy wadze — porównywalne rozmiarem do osobnego FR, patrz pełne uzasadnienie w `FR-83.md`'s sekcji Uwagi.

- **FR-27/v2 (2026-08-11, druga runda tego dnia)**: naprawiono realny błąd zgłoszony przez użytkownika — to samo danie zaplanowane na więcej niż jeden dzień w tym samym tygodniu było liczone tylko RAZ (druga i kolejne obecności traktowane jak duplikat i pomijane), więc np. banan potrzebny do dania zaplanowanego we wtorek i w piątek trafiał na listę w ilości na JEDNO danie zamiast dwa. Bug istniał identycznie na obu platformach — w Kotlinie był to nawet świadomy port web'owego zachowania (`ShoppingOperations.addDayPlanWithSummary`'s stary komentarz: "so a dish planned twice in the same day/week is still only ever added once"), który okazał się błędem, nie zamierzonym zachowaniem. Naprawione tym samym wzorcem na obu platformach: zamiast sprawdzać "czy dodane" na bieżąco w trakcie pętli (co widziało własne dodania z wcześniejszych iteracji TEJ SAMEJ operacji), robi się jeden zrzut stanu PRZED całą operacją — każde zaplanowanie w bieżącym wywołaniu liczy się osobno, powtórne wywołanie tego samego przycisku pozostaje idempotentne. Android: `ShoppingOperations.kt`'s nowa prywatna `addOccurrences`, dwa nowe/zaktualizowane testy JUnit (`ShoppingOperationsTest.kt`). Web: nowa współdzielona `addPlannedOccurrencesToShopping()`, zweryfikowana bezpośrednio w przeglądarce (`javascript_tool`): to samo danie zaplanowane na 2 dni dało 4 jajka zamiast 2, powtórne wywołanie tej samej operacji poprawnie nie podwoiło dalej (idempotencja zachowana). Pełny opis w FR-27/v2.

- **FR-78/v9 + napis oceniania przesunięciem (2026-08-11, druga runda tego dnia)**: (1) Naprawiono realny błąd wydajności na web — KAŻDE zdarzenie `onSnapshot` odpalało pełne 29-polowe porównanie scalające i pełne przebudowanie 229 kart przepisów niezależnie od tego, co się zmieniło, co przy aktywnym drugim urządzeniu (np. Android używany w tym samym czasie) dawało wrażenie ciągłego zacinania się PWA. Naprawione debounce'em (1,2s) na odbiorze zmian + pominięciem przebudowania listy przepisów, gdy zmiana nie dotyczy niczego recepturowego — pełny opis w FR-78/v9. Android NIE miał tego problemu (Compose recomponuje tylko to, co faktycznie się zmieniło, nie ma odpowiednika kosztownego "przebuduj 229 kart"), więc nie wymagał portu. (2) Napis podczas przesuwania karty w Kotlinie ("❤️ LUBIĘ"/"👎 NIE LUBIĘ") ujednolicony z web'em na "Podoba się to dla mnie!"/"Nie podoba się to dla mnie!" (`RecipeListScreen.kt`), plus doczyszczona zapomniana stara wersja tego samego napisu w podpowiedzi web'owych Ustawień.

- **FR-78 (uproszczony port, 2026-08-11, na wyraźną prośbę użytkownika)**: użytkownik zapytał wprost, czy zamiast pełnego web'owego 3-way merge z oknem konfliktu ("czy da się zrobić że ostatni zapis wygrywa... na podstawie logu zmian") wystarczyłoby last-write-wins oparte na tym, co faktycznie się zmieniło. Zaimplementowano `lastKnownFields` w `CloudSyncCoordinator.kt` — mapę "ostatnia wartość każdego pola, o której to urządzenie i Firestore się zgodziły", aktualizowaną (a) optymistycznie w momencie wysyłki push (jeszcze przed odpowiedzią sieci, ta sama filozofia co istniejący `lastPushed`) i (b) po każdym odebraniu zdalnego snapshotu, dla KAŻDEGO pomyślnie zdekodowanego pola, niezależnie czy zostało faktycznie zaaplikowane lokalnie. Każdy push teraz woła `SetOptions.mergeFields(...)` z listą OGRANICZONĄ wyłącznie do pól, które faktycznie różnią się od `lastKnownFields` — nie ze statyczną listą wszystkich 19 pól jak dotychczas. To naprawia realną klasę błędu: dotychczas KAŻDY push nadpisywał WSZYSTKIE pola bieżącą lokalną kopią tego urządzenia, więc jeśli urządzenie A było offline (albo jego listener po prostu jeszcze nie nadążył) w momencie, gdy urządzenie B wypchnęło zmianę np. spiżarni, a A następnie zapisało niezwiązaną zmianę np. profilu, poprzedni kod cicho dopisywał też nieaktualną kopię spiżarni A do tego samego zapisu — nadpisując realną, świeższą zmianę B w Firestore bez ostrzeżenia. Teraz push profilu dotyka WYŁĄCZNIE pola `profile`, nigdy `pantry`, więc taka sytuacja nie może już wystąpić. `PushedSnapshot`/`lastPushed` (mechanizm chroniący przed nieuporządkowanymi potwierdzeniami push'y, patrz notatka FR-73 niżej) pozostawiony BEZ ZMIAN — celowo nieużywany do wykrywania konfliktów, żeby nie zaryzykować regresji już zdiagnozowanego błędu. **Świadomie NIE ma okna konfliktu** — jeśli dwa urządzenia edytują DOKŁADNIE to samo pole zanim zobaczą nawzajem swoje zmiany, wygrywa to, które dotrze do serwera Firestore jako ostatnie, całkowicie po cichu; uznane za akceptowalny kompromis dla aplikacji jednoosobowej (rzadkość jednoczesnej edycji dokładnie tego samego pola w kilkusekundowym oknie), zamiast budować pełne UI konfliktu jak w web. Zweryfikowane: pełna kompilacja + `./gradlew test`/`testDebugUnitTest` (wszystkie zielone, żaden istniejący test nie wymagał zmiany, bo publiczne zachowanie push/pull się nie zmieniło, tylko to, KTÓRE pola trafiają w danym zapisie) — weryfikacja na dwóch prawdziwych, jednocześnie działających urządzeniach nie była możliwa w tym środowisku (jak w wielu innych notatkach wyżej), tylko przez analizę kodu i testy jednostkowe.

- **Naprawiony błąd aktualizacji APK (2026-08-11)**: „Sprawdź aktualizację” w kółko oferowało/instalowało wersję 0.1.41, mimo że `dist/version.json` deklarował 0.1.42 — przyczyna: w poprzedniej turze APK skopiowany do `dist/app-debug.apk` został zbudowany PRZED bumpem `versionCode`/`versionName` w `build.gradle.kts`, nie po (Gradle nie przebudowuje niczego samo z siebie tylko dlatego, że plik się zmienił) — potwierdzone bezpośrednio przez `aapt dump badging dist/app-debug.apk`, który pokazał `versionCode='42'` mimo że plik `version.json` obok deklarował 43. Naprawione: przebudowa APK PO bumpie do 44/"0.1.43", zweryfikowane `aapt dump badging` na KAŻDYM kroku (build output → skopiowany plik w `dist/`) przed commitem, zamiast ślepo ufać, że plik na dysku jest aktualny. Zaktualizowano też pamięć sesji (`feedback_android_dist_release`) z dokładną kolejnością kroków, żeby to się nie powtórzyło.

- **Synchronizacja między urządzeniami — zgłoszenie użytkownika (2026-08-11)**: "aplikacja kotlin otwarta na dwóch różnych urządzeniach... odświeża się niemal natychmiastowo. Gorzej jest z aplikacją pwa... to tak jakby dwa różne profile." Zbadane, ale NIE odtworzone bezpośrednio (wymaga dwóch prawdziwych zalogowanych urządzeń, których to środowisko deweloperskie nie ma) — kod Firebase Auth po stronie web (`signInWithPopup`) i Android (natywny Google Sign-In wymieniany na `GoogleAuthProvider.getCredential`) prowadzą, przy tym samym projekcie Firebase, do TEGO SAMEGO `uid` dla tego samego konta Google — to standardowe, sprawdzone zachowanie Firebase Auth, nie znaleziono w kodzie nic, co by to psuło. Najbardziej prawdopodobne wyjaśnienia: (a) urządzenie webowe faktycznie NIE jest zalogowane na prawdziwe konto (nadal anonimowe — łatwe do sprawdzenia: „Ustawienia → Konto w chmurze” powinno pokazywać ten sam e-mail na obu platformach, nie „zalogowano anonimowo”), (b) resztki dzisiejszego, już naprawionego błędu nieskończonej pętli synchronizacji (patrz FR-78/v5/v7 wyżej) — jeśli przeglądarka była w tej pętli, mogła nie zdążyć realnie zastosować/wyrenderować prawdziwych zmian z drugiego urządzenia zanim użytkownik uznał, że "nie synchronizuje się", (c) przeglądarkowa karta zamknięta/w tle w momencie zmiany na drugim urządzeniu — `onSnapshot` nasłuchuje tylko, gdy karta faktycznie ma uruchomiony JS, w odróżnieniu od natywnej aplikacji Android. Poproszono użytkownika o sprawdzenie (a) jako najszybszej, najbardziej prawdopodobnej przyczyny — patrz podsumowanie na koniec tej tury.

- **FR-3/FR-78 (nadal web-only)**: (1) FR-3/v4: dwuetapowe otwieranie karty przepisu (pierwsze dotknięcie centruje, drugie rozwija; wyrównanie do góry zamiast centrowania dla kart wyższych niż ekran) — w Kotlinie `RecipeListWithScrollToTop`'s istniejący `LaunchedEffect(expandedRecipeId)` centruje NATYCHMIAST po rozwinięciu (jednoetapowo), wymaga analogicznego `pendingCenterCard`-owego stanu pośredniego; jeszcze NIE przeniesione. (2) FR-78/v6/v7: małe kółeczko synchronizacji + wyłącznik bezpieczeństwa w nagłówku web'a — Android nadal nie ma ŻADNEGO UI stanu synchronizacji (patrz notatka FR-73 niżej), i (mając last-write-wins zamiast pełnego 3-way merge) nie ma tej samej klasy błędu pętli synchronizacji, więc wyłącznik bezpieczeństwa akurat nie jest tam pilny — ale widoczny wskaźnik nadal byłby użyteczny.

- **FR-44/v2 + FR-45/v4 + Google-search-link + błąd aktualizacji (2026-08-11)**: cztery drobne poprawki na wyraźną prośbę użytkownika, wszystkie w Kotlinie, wszystkie tego samego dnia. (1) Nagłówek na Przepisach pokazuje się teraz WYŁĄCZNIE blisko góry listy (`HeaderScrollBehavior.isNearTop`, `scrolledDown`/`scrolledUp` usunięte jako martwy kod razem z testami), a ręczne przełączenie w KTÓRĄKOLWIEK stronę zamraża automatykę (`headerAutoFrozen = true` bezwarunkowo, nie tylko przy zwinięciu) — dokładny port web'owego FR-44/v2+FR-45/v4 z tej samej daty. (2) Dotknięcie tytułu przepisu otwiera wyszukiwanie Google (`"${recipe.name} przepis"`) w przeglądarce, port dotąd nieudokumentowanej (brak osobnego FR) web'owej funkcji `.recipe-title`'s click handler — tytuł podkreślony wizualnie jako wskazówka, że to link. (3) **Naprawiono realny błąd**: „Sprawdź aktualizację” w kółko oferowało/instalowało wersję 0.1.41 mimo że `dist/version.json` deklarował 0.1.42 — przyczyna: APK skopiowany do `dist/app-debug.apk` w poprzedniej turze był zbudowany PRZED bumpem `versionCode`/`versionName`, nie po (potwierdzone `aapt dump badging`: plik faktycznie miał `versionCode='42'` mimo że `version.json` obok niego mówił 43). Naprawione przebudowaniem APK PO bumpie (44/"0.1.43"), zweryfikowane `aapt` na każdym kroku (build output → `dist/`) przed commitem — patrz też zaktualizowana pamięć `feedback_android_dist_release` z dokładną kolejnością kroków na przyszłość.

- **FR-1** (5 kategorii): dane i kafelki kategorii są w Kotlinie, przepisy działają; Planer ma już własny ekran (FR-18), zakładka Postęp dostała pierwszą realną zawartość 2026-08-10 (patrz notatki FR-60/FR-37/FR-40 i FR-41/FR-42 niżej) — już nie placeholder, FR-41/42 też gotowe; FR-38/39 (powiadomienia o wodzie) dokończone 2026-08-11 — nie ma już żadnego znanego pozostałego braku funkcjonalnego względem web (patrz notatka FR-38/39 niżej dla drobnych, świadomych różnic natywnej implementacji).
- **FR-2 (dokończone 2026-08-11)**: pole wyszukiwania, sortowania 🎯/❤️/🏆 (FR-11/FR-57/FR-67) już działały; w tej turze dodane pozostałe: **⭐ ulubione przepisy** — zupełnie NOWY koncept (`RecipeViewModel._favoriteRecipes`, `Set<String>` id-ków, port `state.favorites[r.id]`), odrębny od istniejącego `FavoriteIngredientsViewModel` (ulubione SKŁADNIKI); nowy przycisk ☆/★ w nagłówku każdej karty przepisu (`RecipeCardBody`, obok plakietki „✍️ Twój przepis”), trwale zapisywany przez `LocalPersistenceCoordinator` pod kluczem `"favorites"` (reużywa `CloudSyncCodec.encodeFavIngredients`/`decodeFavIngredients` — identyczny kształt `Set<String>`→`{nazwa:true}`, inny klucz najwyższego poziomu), świadomie NIE zsynchronizowany jeszcze z chmurą (ten sam status co `myRecipes`/`recipeReviews`/`weights` — patrz `LocalPersistenceCoordinator`'s własny komentarz). **🌟 ulubione składniki** — filtruje po `favIngredients` już istniejącym w `FavoriteIngredientsViewModel`, `r.ingredients.any{ RecipePantryMatching.parseIngredient(it).canonName in favIngredients }`. **🏺 ze spiżarni** — nowa `RecipePantryMatching.pantryCoverageRatio(recipe, pantryItems)` w `logic/` (testy JUnit) liczy ułamek składników obecnych w spiżarni (ten sam `containsKey` bez sprawdzania ilości co istniejący „have it” wyróżnik na liście składników karty — świadomie NIE odtworzone `qty<=0` wykluczenie z web'owego `pantryMatch`, dla spójności z tą już istniejącą konwencją), próg ≥60% jak w `index.html`. **🧑‍🍳 moje przepisy** — filtruje `source == "custom"`; **świadomie węższe niż FR-2.md's własny opis** („`state.myRecipes` ORAZ zatwierdzone przepisy społeczności”) — przepisy społeczności to FR-76, wciąż ⬜ w Androidzie (wymaga reguł Firestore w konsoli), więc ten przełącznik na razie pokazuje tylko własne przepisy, nie community. **Próg oceny gwiazdkowej** — cztery dodatkowe `FilterChip`e („Dowolna ocena”/„★ 3+”/„★ 4+”/„★ 5”) zamiast natywnego rozwijanego menu z opisu FR — świadoma adaptacja pod Androida, spójna stylistycznie z resztą paska filtrów (ten sam wzorzec co pozostałe 7 przełączników), filtruje `reviews[r.id]?.stars >= próg`. Wszystkie przełączniki niezależne i łączalne (ten sam `remember(...)` z sekwencyjnym `if(...) result = result.filter{...}` co index.html's `renderRecipes()`, w tej samej kolejności: ulubione → ulub. składniki → spiżarnia → moje przepisy → próg oceny → sortowania). Ręcznie zweryfikowane na emulatorze 2026-08-11: dotknięcie ☆ przy „Szakszuka ze szpinakiem i pomidorami” zmieniło je na ★; dotknięcie „⭐ Ulubione” zawęziło listę do WYŁĄCZNIE tej jednej karty (▸ „Omlet z awokado…” zniknął); `am force-stop` + ponowne uruchomienie pokazało ★ nadal ustawioną (trwałość potwierdzona), a przełącznik „⭐ Ulubione” poprawnie wrócił do stanu wyłączonego (stan przełącznika, w odróżnieniu od samych ulubionych, nie jest zapisywany — dokładnie jak web, gdzie to też zwykła zmienna JS zerowana przy każdym odświeżeniu strony). Zero crashy.
- **FR-3 (dwa prawdziwe braki naprawione 2026-08-11, nie tylko "do potwierdzenia")**: przy okazji sprawdzania kryteriów akceptacji FR-3.md wykryto, że poprzedni stan wcale nie był "gotowe, czeka na weryfikację" — brakowały dwa kryteria z v3. **(1) "Tylko jedna karta rozwinięta jednocześnie"**: `expanded` był poprzednio lokalnym `remember` WEWNĄTRZ `RecipeCard`, więc każda karta pamiętała swój stan niezależnie — dało się rozwinąć dowolnie wiele kart naraz, wprost naruszając to kryterium. Naprawione podniesieniem stanu do `RecipeListWithScrollToTop` jako `expandedRecipeId: String?` (jeden wspólny stan dla całej listy, przekazywany w dół jako `isExpanded`/`onToggleExpanded`) — rozwinięcie karty B automatycznie zwija kartę A, przy okazji też odporniejsze na resetowanie stanu przy scrollowaniu daleko poza okno kompozycji `LazyColumn` (wspólny `remember` w rodzicu nigdy nie jest usuwany z drzewa, w odróżnieniu od poprzedniego per-elementowego). **(2) auto-wyśrodkowanie po rozwinięciu (kryterium z v3)**: było całkowicie nieported — nowy `LaunchedEffect(expandedRecipeId)` czyta `listState.layoutInfo.visibleItemsInfo` po dwóch `withFrameNanos` (żeby złapać już-po-rozwinięciu wysokość karty, nie sprzed), liczy środek elementu i przewija (`animateScrollBy`/`scrollBy` z FR-50's `reducedMotion`) tak, by środek karty pokrył się ze środkiem widocznego obszaru — nie ma tu żadnej `AnimatedVisibility`/`animateContentSize` do doczekania (body karty to zwykłe `if(expanded)`), więc nie trzeba czekać na koniec animacji, tylko na kolejny przebieg layoutu. Ręcznie zweryfikowane na emulatorze 2026-08-11: dotknięcie karty „Omlet z awokado…” podczas gdy „Szakszuka…” była rozwinięta natychmiast zwinęło Szakszukę i rozwinęło Omlet (potwierdzone zniknięciem „pomidorów krojonych”/„garść szpinaku” z drzewa UI i pojawieniem się „awokado”/„roszponką”) — kryterium pojedynczego rozwinięcia działa poprawnie. **Zastrzeżenie**: dla wyjątkowo długich kart (dużo składników + pełny opis makro) wyśrodkowanie bywa tylko przybliżone, bo scroll wywołany przez tę samą akcję jednocześnie odpala FR-44's automatyczne chowanie nagłówka na przewijanie w dół — nagłówek kurczy się W TRAKCIE tego samego gestu, zmieniając dostępną wysokość widoku już PO obliczeniu przewinięcia, więc finalna pozycja jest bliska środka, ale nie zawsze piksel w piksel; kryterium FR-3 „użytkownik nie musi ręcznie doprzewijać, żeby zobaczyć składniki i sposób przygotowania” jest mimo to spełnione (potwierdzone zrzutem ekranu — składniki i przygotowanie w pełni widoczne bez dodatkowego przewijania), tylko górna krawędź karty (tytuł) czasem ląduje tuż poza widokiem zamiast idealnie na środku. Zero crashy w całej rundzie testów.
- **FR-66 (dokończone 2026-08-11, auto-obliczanie makro)**: brakujący kawałek z pierwszego portu (2026-08-10) — `IngredientMacroDb.kt` w `logic/`, wygenerowany skryptem Node (nie przepisany ręcznie, ten sam powód co `SnackNutritionDb`/FR-33-34: zero ryzyka błędu transkrypcji) ze 105 pozycji `INGREDIENT_MACRO_DB` z `index.html`. Nowy `IngredientMacroEstimation.kt` — jeden-do-jednego port `estimateIngredientMacros`/`estimateRecipeMacrosFromText`, reużywający `RecipePantryMatching.parseIngredient` (ta sama funkcja co FR-15/16/25) zamiast osobnego parsowania (`parseIngredient`'s `canonName` jest już w pełni skanonizowany, w odróżnieniu od web'owego `p.name` wymagającego osobnego wywołania `pantryCanon()` — ta sama konwencja co każdy inny konsument tej funkcji w tym module). `AddCustomRecipeDialog` w `RecipeListScreen.kt`: cztery flagi `kcalDirty`/`proteinDirty`/`carbsDirty`/`fatDirty` (port `arMacroDirty`) — `LaunchedEffect(ingredientsText)` na każdą zmianę pola składników przelicza i wypełnia kcal/białko/węgle/tłuszcz TYLKO te pola, w które użytkownik jeszcze ręcznie nie wpisał własnej wartości; podpowiedź pod polem składników zmienia się między trzema stanami („Wpisz składniki…” / „Rozpoznano X z Y…” / „Nie rozpoznano żadnego z X…”), dokładnie jak `recalcAddRecipeMacros`/`arAutoCalcHint`. Testy JUnit (`IngredientMacroEstimationTest.kt`, 8 przypadków, w tym spot-checki wartości i sumowanie wielu linii) — jeden przypadek złapał realny, ten sam rodzaj quirku co już udokumentowany w `SnackNutritionDbTest`: klucz `"kurczak (pierś)"` nie jest osiągalny przez wpisanie go dosłownie (`coreName` obcina nawias), trzeba wpisać samo „kurczak” — nie błąd portu, tylko własność współdzielonych tabel kanonizacji, testy dostosowane zamiast cicho pomijane. Ręcznie zweryfikowane na emulatorze 2026-08-11: wpisanie „jajka” w pole składników na żywo wypełniło Kalorie=„78”, Białko=„6.3”, Węgle=„0.6”, Tłuszcz=„5.3” (dokładnie wartości z bazy/testów) z podpowiedzią „Rozpoznano 1 z 1 składników — obliczono na tej podstawie (możesz poprawić ręcznie)”. Zero crashy.
- **FR-13** (5. kategoria Deser/Przekąska, poprawiona notatka 2026-08-11): poprzednia wersja tej notatki myliła FR-13 z brakiem hipotetycznych "funkcji per-kategoria" (których FR-13 wcale nie wymaga — to nie ta funkcja). Faktyczne 3 kryteria akceptacji FR-13.md sprawdzone bezpośrednio w źródłach: (1) `recipes.json` (ten sam plik co web, `PowerShell ConvertFrom-Json | Group-Object cat` policzone wprost) ma dokładnie 16 przepisów `cat:"deser"` z 229 ogółem; (2) `ProfileCalculations.kt`'s `DESER_RATIO = 200.0/1500.0` — dokładnie skorygowany udział z FR-13/v2 (nie błędny pierwotny 100/1500, który dawał systemowe 0% dopasowania), już w tej wartości od pierwszego portu FR-6/7/10 (web'owy plik już był poprawiony, zanim Android w ogóle powstał); (3) skoro dane (16 przepisów) i formuła podziału budżetu kalorycznego są identyczne z już-poprawioną wersją web, a `RecipeMatching.matchScore` jest wiernym portem tej samej matematyki dopasowania (FR-11, pokryte testami JUnit) — sensowne, niezerowe dopasowanie dla deserów wynika z tego samego mechanizmu, nie wymaga osobnego kodu do zweryfikowania. Wszystkie 3 kryteria spełnione bez dodatkowej pracy — status podniesiony do ✅.
- **FR-26 (dokończone, 2026-08-10)**: odhaczanie i "Usuń kupione" już działały; dwa pozostałe kawałki dodane w tej turze. Nowe `ShoppingOperations.clearAll` (`logic/`, testy JUnit) — w odróżnieniu od `clearChecked` usuwa WSZYSTKO niezależnie od stanu odhaczenia; index.html przy okazji zeruje osobną mapę `state.recipeAdded`, ale Android w ogóle nie ma takiego osobnego pola — `isRecipeAdded` liczy się na żywo z `items`, więc samo wyczyszczenie `items` już w pełni odznacza każdy przepis, bez dodatkowego kroku. `ShoppingOperations.buildShareText` — port `buildListText()`: tylko nieodhaczone pozycje, pogrupowane, posortowane w grupie po nazwie, pusta lista → "Lista zakupów jest pusta 🎉"; świadomie używa WŁASNEGO, już istniejącego 8-kategoriowego grupowania Androida (`IngredientCanon.CANON_INFO`/`PantryTiles.CATEGORY_ORDER`, to samo co siatka kafelków Spiżarni) zamiast portowania osobnej, keyword'owej funkcji `classify()` z web — to samo zadanie (czytelny podział na kategorie), jeden mniej równoległy system kategoryzacji do utrzymania. `ShoppingScreen.kt`: przycisk „📤 Udostępnij" woła `Intent.ACTION_SEND`/`Intent.createChooser` (natywny odpowiednik web'owego `navigator.share()`/SMS/WhatsApp/kopiuj — jeden systemowy selektor zamiast czterech osobnych przycisków, bo Android i tak pokazuje wszystkie zainstalowane aplikacje do udostępniania w jednym miejscu), przycisk „Wyczyść całą listę" z `AlertDialog` potwierdzenia (port `confirm("Na pewno wyczyścić całą listę zakupów?")`). Ręcznie zweryfikowane na emulatorze 2026-08-10: „Udostępnij" na liście z prawdziwymi pozycjami otworzyło systemowy selektor Androida z poprawnie pogrupowanym tekstem („🛒 Lista zakupów:\n\nWarzywa:\n- 1 czosnek\n- 0.5 pomidor\n- 1 szpinak\n\nMięso, ryby, jajka:…"); okienko „Wyczyścić całą listę zakupów?" wyrenderowało się poprawnie z przyciskami Anuluj/Wyczyść. Zero crashy. `versionCode` 32→33, `versionName` 0.1.31→0.1.32.
- **FR-28/FR-30 (przebudowa 2026-08-10)** — Spiżarnia przebudowana od zera z listy-tylko-śledzonych-pozycji na pełną siatkę kafelków dla KAŻDEGO składnika występującego w jakimkolwiek przepisie (jeden-do-jednego port `buildPantryTileList()`/`renderPantry()`/`tileTapDelta()` z `index.html`), na życzenie użytkownika po tym jak zgłosił że stary ekran "nie działa wcale". `PantryTiles.kt` (nowy plik w `logic/`, testy JUnit): `buildTileNames(recipes)` — deduplikowany zbiór kanonicznych nazw ze WSZYSTKICH przepisów (`IngredientCanon.coreName`+`pantryCanon`, ta sama funkcja co FR-4/FR-16/FR-25 już używały); `computeTileUnitCats(recipes)` — głosowanie większościowe domyślnej jednostki per składnik przez `RecipePantryMatching.parseIngredient` (port `computeTileUnitCats`/`TILE_UNIT_CAT`, zawężony do uproszczonego modelu weight/volume/count zamiast 11 kategorii jednostek z web); `categoryAndEmoji(name)` — lookup `IngredientCanon.CANON_INFO` z fallbackiem „Inne”/🍽️ jak w źródle; `CATEGORY_ORDER` — 8 kategorii (`PantryCategory` w `data/PantryItem.kt` przepisany z 6/7 uproszczonych etykiet na dokładnie te same 8 co web: Nabiał/Warzywa/Owoce/Mięso, ryby, jajka/Strączki i orzechy/Pieczywo i zboża/Przyprawy/Inne — te same etykiety, których `CANON_INFO.cat` już używał, więc żadnego osobnego mapowania nie trzeba było zgadywać). `SpiceLevel` przepisany z `Brak/Mało/Wystarczy` (3 poziomy z opakowanym cyklem, włącznie z auto-usuwaniem) na dokładnie `Mało/Wystarczy/Dużo` jak w źródle — BEZ poziomu „Brak” (nieśledzona przyprawa to po prostu brak wpisu w mapie, nie krok w cyklu). `PantryOperations.tileTapDelta(items, name, category, unitCat, dir)` — jeden-do-jednego port `tileTapDelta()`: `dir=+1` (górna połowa kafelka) na nieśledzonym tworzy wpis (produkt przy `tileStep`, przyprawa na „Mało”); `dir=-1` (dolna połowa) na nieśledzonym jest no-opem (nie da się „zejść poniżej zera” przed pierwszym dotknięciem); produkt zatrzymuje się na ilości 0 ale ZOSTAJE śledzony (tylko przytrzymanie → „Usuń śledzenie” faktycznie kasuje wpis — to samo dotyczy przyprawy zaciśniętej na „Mało”/„Dużo”, bez zawijania) — świadomie wierny port tej samej „dziwności” źródła, nie błąd. `PantryScreen.kt` przebudowany na `LazyVerticalGrid` (`GridCells.Adaptive(78.dp)`, nagłówki kategorii jako pełnoszerokie `item(span={GridItemSpan(maxLineSpan)})` w JEDNEJ siatce zamiast osobnych zagnieżdżonych siatek per kategoria — unika problemu zagnieżdżonych scrollowalnych list) — każdy kafelek to `Box` z `Modifier.pointerInput{detectTapGestures(onTap={offset-> if(offset.y<size.height/2f) dodaj else odejmij}, onLongPress={...})}`, dokładny odpowiednik `e.clientY - rect.top < rect.height/2` z web, przy czym Compose'owe `detectTapGestures` samo gwarantuje że długie przytrzymanie nie odpala też zwykłego dotknięcia (to samo `longFired` zabezpieczenie co web robił ręcznie przez `pointerdown`+`setTimeout`). Kafelek „➕ Dodaj własny" na końcu KAŻDEJ kategorii (nawet pustej, poza „Inne" — jak w źródle) otwiera proste okienko z samą nazwą — dodanie działa jak zwykłe `dir=+1` na nowej nazwie (bez osobnego formularza ilości/jednostki/typu, bo to i tak w pełni wynika z kategorii kafelka, na którym stanęło „Dodaj własny"). Długie przytrzymanie ŚLEDZONEGO kafelka otwiera okienko zmiany kategorii/usunięcia śledzenia (świadomie NIE resetuje ilości przy zmianie kategorii, w odróżnieniu od web, które kasuje stan przy każdej zmianie kategorii/jednostki — uznane za usterkę źródła niewartą wiernego powielania, biorąc pod uwagę że to CAŁA ta tura naprawia właśnie „dane znikają niespodziewanie"). Świadomie NIE ported: skanowanie kodu kreskowego (FR-31, kamera — osobny, nieporuszany temat), zmiana jednostki z poziomu długiego przytrzymania (uproszczony 3-kategoriowy model jednostek już i tak ogranicza sens tej funkcji), oraz osobny trwały rejestr `customTiles` (kafelek własny znika z siatki gdy przestaje być śledzony, zamiast zostawać widoczny na zawsze jak w web — akceptowalne uproszczenie). Ręcznie zweryfikowane na emulatorze 2026-08-10 (uważnie, z `uiautomator dump` bezpośrednio przed każdym dotknięciem — to konkretne środowisko/emulator miało skłonność do kolejkowania i opóźnionego odtwarzania starszych zdarzeń dotyku z tej sesji, co dawało mylące wyniki przy pośpiechu, ale przy dumpie tuż przed dotknięciem wyniki były w pełni deterministyczne): świeża, pusta spiżarnia pokazała kafelki dla WSZYSTKICH składników przepisów pogrupowane w kategorie z kafelkiem „Dodaj własny" na końcu każdej; dotknięcie górnej połowy kafelka „cebula” utworzyło wpis produktu (1 szt.) widoczny natychmiast jako plakietka „1” z zieloną obwódką; bezpośredni odczyt `adb shell run-as ... cat shared_prefs/dieta_app_prefs.xml` potwierdził poprawny zapis JSON w nowym schemacie (kategorie/poziomy przypraw); `am force-stop` + ponowne uruchomienie pokazało dokładnie te same śledzone kafelki (w tym te dodane wcześniej w tej samej sesji, obejmujące produkt i przyprawy) — pełna trwałość między restartami potwierdzona dla nowego modelu. Zero crashy mimo wielu (w tym przypadkowych/powielonych przez środowisko) dotknięć w trakcie tej sesji.
- **FR-28 (poprawka 2026-08-10, przycięte plakietki)**: użytkownik zgłosił (ze zrzutami ekranu web vs Android), że plakietki ilości/poziomu przyprawy na kafelkach Spiżarni są wizualnie ucinane w Androidzie (np. „Wystarcz…” zamiast „Wystarczy”), podczas gdy web pokazuje je w całości. Przyczyna: plakietka celowo wystaje poza róg kafelka (`Modifier.align(Alignment.TopEnd).offset(x=6.dp,y=-6.dp)`, odpowiednik web'owego `position:absolute; top:-7px; right:-7px`), ale żyła jako dziecko tego samego `Box`, na którym wołano `Modifier.clip(shape)` do zaokrąglenia rogów kafelka — Compose'owy `clip` przycina WSZYSTKIE dzieci do granic rodzica, więc wystająca plakietka była ucinana razem z resztą. Naprawione przez rozdzielenie warstw: zewnętrzny `Box` (bez `clip`, tylko `pointerInput`/rozmiar) trzyma teraz plakietkę jako zwykłe, nieprzycinane dziecko, a tło/obwódka/zaokrąglenie żyją na osobnym wewnętrznym `Box` (`Modifier.matchParentSize().clip(shape)...`). Przy okazji dodano subtelny `Modifier.shadow(...)` na kafelku i plakietce (miękki cień zamiast płaskiej krawędzi) — jeden z kilku drobnych kroków w stronę „miększego” wyglądu bliższego web'owej wersji, na wyraźną prośbę użytkownika. Ręcznie zweryfikowane na emulatorze 2026-08-10 (`uiautomator dump` + zrzut ekranu): kafelek „sól” po dwóch dotknięciach pokazał plakietkę „Wystarczy” w całości, bez ucięcia, z widocznym miękkim cieniem. Zero crashy.
- **Testy automatyczne**: reguły biznesowe stojące za FR-2 (wyszukiwanie/filtrowanie), FR-26 (odhaczanie/czyszczenie zakupów) i FR-28/FR-30 (`PantryOperations.tileTapDelta`/`changeCategory`/`removeItem`, `PantryTiles.buildTileNames`/`computeTileUnitCats`/`categoryAndEmoji`) są pokryte prawdziwie uruchamianymi testami JUnit w `android/logic/` (`PantryOperationsTest.kt`, `PantryTilesTest.kt`). Patrz `android/README.md`, sekcja "Testy automatyczne".
- **FR-6** (profil + BMR/TDEE): port `calcTargets` z `index.html` jeden-do-jednego (`ProfileCalculations.kt` w `logic/`, testy JUnit z ręcznie przeliczonymi wartościami). Formularz w Ustawieniach (płeć, wiek, wzrost, waga obecna/docelowa, aktywność, cel) + wynik w tej samej karcie + podsumowanie w nagłówku (współdzielony `ProfileViewModel` na poziomie `DietaAppRoot`, nie per-ekran, żeby oba miejsca zawsze zgadzały się co do stanu). Ręcznie zweryfikowane na emulatorze 2026-08-09: zapis (30/170/65/60 → 1540 kcal/dzień, śniadanie 350/II śniadanie 270/obiad 430/kolacja 290/deser 210) i reset do domyślnych (→ 1480 kcal/dzień) dają dokładnie te same liczby co ręczne przeliczenie wzoru Mifflin-St Jeor, zero crashy. Lokalny stan (bez trwałego zapisu/synchronizacji) — jak Spiżarnia/Zakupy, do czasu kroku 6. Makra (FR-10) i dopasowanie przepisu (FR-11) NIE są jeszcze zrobione — to osobne, kolejne FR-y.
- **FR-7**: `calcTargets` zwraca już wszystkie 5 targetów posiłkowych (patrz FR-6 wyżej) — przy okazji poprawiono nieaktualne proporcje w `Functional requirements/FR-7.md`/`ALL-REQUIREMENTS.md` (opisywały 370/280/450/300/100 sprzed dodania Deseru jako pełnoprawnej kategorii; realny kod w `index.html` już dawno używa 340/260/420/280/200).
- **FR-5** (przycisk powrotu do góry): `RecipeListWithScrollToTop` w `RecipeListScreen.kt` — FAB "⬆️" pojawia się po przewinięciu >400dp (albo poza pierwszy element listy) i scrolluje z animacją do indeksu 0. Ręcznie zweryfikowane na emulatorze 2026-08-09: pojawia się przy scrollu, znika z powrotem na górze, `animateScrollToItem` faktycznie płynnie przewija, zero crashy.
- **FR-4** (miniatura = emoji głównego składnika): `IngredientCanon.kt` w `logic/` — port `UNIT_WORDS`/`LEMMA_FIX`/`RAW_TO_CANON`/`CANON_INFO`/`THUMB_CANON_ALIASES`/`THUMB_SKIP_CANON` oraz `coreName`/`pantryCanon`/`thumbCanon`/`mainIngredientInfo` z `index.html` (dane wygenerowane skryptem z tych samych stałych, nie przepisywane ręcznie, żeby uniknąć błędów transkrypcji w ~460 wpisach), testy JUnit w `IngredientCanonTest.kt`. `RecipeCard` w `RecipeListScreen.kt` liczy `IngredientCanon.mainIngredientInfo(recipe)?.emoji` raz per karta (`remember(recipe.id)`) zamiast pobierać zdjęcie z sieci. Ręcznie zweryfikowane na emulatorze 2026-08-09: karty w zakładkach Śniadania i Obiady pokazują zróżnicowane, sensowne emoji dopasowane do głównego składnika (np. 🥛 jogurt/skyr, 🫘 hummus/ciecierzyca, 🥜 orzechy, 🥑 awokado, 🦃 indyk, 🍞 pieczywo, 🍠 batat), bez połączenia z siecią, zero crashy.
- **FR-8/FR-9** (filtry glutenu/laktozy/niskiego IG): `RecipeBrowsing.isGlutenFree`/`isLactoseFree` to jeden-do-jednego port `GLUTEN_KEYWORDS`/`DAIRY_KEYWORDS` z `index.html`, wpięte w `visibleRecipes` i sterowane checkboxami w karcie Profilu (`ProfileViewModel` współdzielony, więc filtr w Ustawieniach od razu wpływa na ekran Przepisów). Ręcznie zweryfikowane na emulatorze: wyszukanie "chleb" dawało kilka wyników bez filtra, "Brak przepisów spełniających kryteria" z aktywnym filtrem bezglutenowym, zero crashy. `strictLowGI` było pierwotnie tylko polem w profilu i checkboxem w UI bez żadnego efektu, dopóki nie powstało `RecipeMatching.matchScore` (FR-11) — **notatka nieaktualna od tamtej pory, poprawiona 2026-08-11**: `matchScore` faktycznie konsumuje ten flag (`RecipeMatching.kt` linia ~50) dokładnie tak jak opisano — gdy `profile.strictLowGI` i ładunek glikemiczny (`recipe.gl`) przekracza próg zależny od celu (20 dla redukcji/utrzymania, 35 dla budowania), wynik dopasowania 🎯 dostaje karę proporcjonalną do przekroczenia (waga 1.5 dla redukcji/utrzymania, 0.6 dla budowania, łagodniej bo węglowodany są tam pożądane) — jeden-do-jednego port tej samej kary z `index.html`'s `recipeMatchScore`. W pełni pokryte testami JUnit (`RecipeMatchingTest.kt`: kara włączona/wyłączona, próg zależny od celu, klamrowanie wyniku do [0,100]) — silniejsza weryfikacja niż pojedynczy zrzut ekranu dla wyliczenia liczbowego, więc status podniesiony do ✅ bez dodatkowej rundy na emulatorze.
- **FR-10** (proporcje makro): `ProfileCalculations.calcMacroTargets` — jeden-do-jednego port `MACRO_RATIOS`/`calcMacroTargets` z `index.html`. Wynik (białko/węgle/tłuszcz na dzień) doklejony do tego samego komunikatu "Dopasowano..." po zapisaniu profilu. Ręcznie zweryfikowane na emulatorze: domyślny profil (kobieta/redukcja) dał dokładnie 111 g białka / 130 g węglowodanów / 58 g tłuszczu — identycznie jak w teście JUnit, zero crashy.
- **FR-11** (dopasowanie 🎯): `RecipeMatching.matchScore` — jeden-do-jednego port `recipeMatchScore` z `index.html` (zbieżność B/W/T z targetem kategorii + kara GL zależna od `strictLowGI`/celu, testy JUnit z ręcznie przeliczonymi wartościami dla obu gałęzi kary). Plakietka "🎯 N%" na karcie przepisu + przełącznik "🎯 Dopasowanie" w pasku filtrów, sortujący malejąco (FR-2). Ręcznie zweryfikowane na emulatorze: plakietki pojawiły się na wszystkich kartach z pełnymi danymi odżywczymi, sortowanie dało poprawnie malejącą kolejność (73%, 73%, 72%, 71%...), zero crashy.
- **FR-14** (skalowanie UI): `UiScale.detectDefault` — jeden-do-jednego port `detectDefaultUiScale` z `index.html` (interpolacja 0.75–1.0 dla 360–420dp szerokości, zaokrąglenie do kroku 0.05), testy JUnit. Compose nie ma odpowiednika CSS `zoom`, więc efekt uzyskano skalowaniem `LocalDensity` wokół całego drzewa w `MainActivity.setContent` (obejmuje to też TopAppBar i NavigationBar, jak zoom obejmował `position:fixed` w wersji webowej) — patrz komentarz w kodzie. Suwak w karcie "🔍 Rozmiar aplikacji" (zakres 0.7–1.3, krok 0.05). Ręcznie zweryfikowane na emulatorze: auto-detekcja dała sensowną wartość (95% dla tego ekranu), przeciągnięcie suwaka do 100% natychmiast przeskalowało cały interfejs (widocznie większy tekst/odstępy), zero crashy. **AKTUALIZACJA 2026-08-10**: trwały zapis dodany — patrz notatka „Trwały zapis lokalny" niżej. `uiScale` przetrwa teraz restart aplikacji jak reszta zsynchronizowanego stanu; status podniesiony do ✅.
- **FR-15/FR-17** (historia gotowania + oceny gwiazdkowe): `CookEntry`/`CookHistoryOperations` w `logic/` (dodanie wpisu „dzisiaj”, ustawienie/wyczyszczenie oceny, usunięcie wpisu — testy JUnit) + `RecipePantryMatching` w `logic/` (port `parseIngredient`/`subtractRecipeFromPantry`/`restoreRecipeToPantry` z `index.html`, ograniczony do kategorii waga/objętość/sztuki, jakie wolnotekstowa jednostka spiżarni w Androidzie realnie wyraża — patrz uwaga w `PantryItem.kt` o uproszczonym modelu; testy JUnit). `PantryViewModel` przeniesiony na poziom `MainActivity` (współdzielony z `ProfileViewModel`), żeby odjęcie/przywrócenie składników na zakładce Przepisy było od razu widoczne na zakładce Spiżarnia. Przycisk „✅ Zrobione (N×)” na karcie przepisu zawsze otwiera okienko historii (nigdy nie oznacza od razu jako zrobione — zgodnie ze zrewidowanym FR-15), z przyciskiem „Zrobione dzisiaj”, listą wpisów (data + 5 gwiazdek na pełną szerokość wiersza, FR-17) i usuwaniem wpisu przez potwierdzenie (`AlertDialog`, tekst jeden-do-jednego z `index.html`). Ręcznie zweryfikowane na emulatorze 2026-08-09: okienko zawsze pokazuje historię (nie oznacza „na sucho”), licznik „(N×)” aktualizuje się poprawnie, wiele niezależnych wpisów z osobnymi ocenami i poprawnie liczoną średnią, usunięcie wpisu po potwierdzeniu cofa ocenę i przywraca składniki, oraz pełny cykl odjęcia/przywrócenia w spiżarni sprawdzony wprost: dodanie „jajka: 10 szt.” w Spiżarni → oznaczenie Szakszuki (2 jajka) jako zrobionej → 8 szt. w Spiżarni → usunięcie wpisu → z powrotem 10 szt. Zero crashy. FR-16 (osobne okienko „🏺 Sprawdź stan spiżarni dla tego dania”) to inny przycisk, jeszcze nie ported.
- **FR-18/19/20** (Planer: siatka dni×slotów, wybór z karty przepisu, skalowanie porcji): `PlannedMeal`/`PlannerOperations`/`WeekPlan` w `logic/` (testy JUnit) — port `state.planner`/`state.plannerScale`, `SCALE_STEPS`, `idealScaleFor`/`nextScaleStep`/`scaleRecipe`'s kcal z `index.html`. Nowy `PlannerScreen.kt` (zastąpił placeholder) renderuje 7 kart dni × 5 wierszy slotów (`PlannerOperations.PLANNER_CATEGORIES` — te same `cat` co dane przepisów, ale osobne Śniadanie/II Śniadanie, inaczej niż połączona zakładka przeglądania z FR-74), z sumą kcal dnia na dole karty. Dotknięcie slotu otwiera `PlannerSlotDialog` — listę przepisów danej kategorii posortowaną wg `RecipeMatching.matchScore` malejąco, plus „— brak / wyczyść —” gdy slot jest wypełniony (uproszczenie względem `index.html`, które ma osobny ekran „szczegóły” i „wybór” — tu połączone w jeden, bo skala/resztki żyją na wierszu karty dnia, nie w osobnym modalu). FR-19: nowy przycisk „📅 Zaplanuj” na karcie przepisu (obok „✅ Zrobione”) otwiera `PlanPickerDialog` — siatkę kategorii (domyślnie kategoria przepisu, zmienialna) i siatkę 7 dni, oba jako `FlowRow` (**nie** zwykły `Row`+`chunked` — patrz błąd niżej), tapnięcie dnia od razu zapisuje wybór. `PlannerViewModel` przeniesiony na poziom `MainActivity` (jak Pantry/Shopping), więc wybór z karty przepisu jest natychmiast widoczny w zakładce Planer. FR-21 do FR-24 i FR-27 (losowanie, czyszczenie, resztki „ugotuj na 2 dni”, proaktywna podpowiedź, zbiorcze dodanie składników z tygodnia) świadomie NIE są jeszcze ported — to następny, osobny krok. Napotkany i naprawiony bug: pierwsza wersja siatek kategorii/dni w `PlanPickerDialog` używała zwykłego `Row` z ręcznie dzielonymi wierszami (`chunked(3)`/`chunked(4)`) — Compose `Row` nie zawija się automatycznie, więc element, który nie mieścił się w dostępnej szerokości (długie polskie nazwy dni, np. „Poniedziałek”), dostawał zerowej szerokości wymuszenie i renderował się jako kolumna pojedynczych liter w bardzo wąskim, bardzo wysokim czipie zamiast zawinąć się do nowego wiersza — naprawione przez `FlowRow` (`ExperimentalLayoutApi`), które faktycznie zawija elementy według rzeczywistej zmierzonej szerokości. Ręcznie zweryfikowane na emulatorze 2026-08-09: pusty slot pokazuje „— wybierz danie —”, wybór z listy posortowanej wg dopasowania natychmiast aktualizuje „Razem: N kcal” (425 kcal → 531 kcal po zmianie skali na 1,25×, dokładnie 425×1.25 zaokrąglone), wybór „📅 Zaplanuj” z karty przepisu (Przepisy, kategoria domyślna = kategoria przepisu) na inny dzień/kategorię natychmiast widoczny w zakładce Planer (Środa/Śniadanie pokazało „Razem: 320 kcal” dokładnie odpowiadające zaplanowanemu przepisowi), siatki kategorii/dni renderują się poprawnie na kilku liniach bez paska przewijania po naprawie `FlowRow`. Zero crashy.
- **FR-21/22/23/24** (losowanie, czyszczenie, resztki, proaktywna podpowiedź): `PlannerOperations` w `logic/` — `fittingPool` (top ~20%, min 3, wg `RecipeMatching.matchScore`, port `fittingPool`/`isPrepAheadFriendly`/`MEAL_PREP_KEYWORDS` z `index.html`), `randomizeDay`/`randomizeWeek` (round-robin po potasowanej puli per kategoria — dokładnie ten sam algorytm co `autoPlanBtn` w web, żeby mała pula i tak dawała różnorodność zamiast powtarzać tę samą pozycję), `regenerateSlot` (,,🔁'', omija bieżący wybór gdy jest alternatywa), `clearDay`, `planLeftover`, `prepAheadSuggestion` — wszystko z testami JUnit (w tym `Random` z ziarnem dla deterministycznych testów losowania). `PlannerScreen.kt`: przycisk „🎲 Wygeneruj losowo cały tydzień” na górze listy, „🎲 Losuj ten dzień”/„🗑️ Wyczyść ten dzień” na każdej karcie dnia (oba z potwierdzeniem przez `AlertDialog`, treść z nazwą dnia), „🔁” przy każdym wypełnionym slocie, baner podpowiedzi „🍱 Ugotowano na więcej dni w {dzień}: {danie} — powtórzyć jako resztki?” pod pustym slotem gdy `prepAheadSuggestion` zwróci danie, i przycisk „🍱 Ugotuj na 2 dni” w oknie slotu (widoczny przy skali ≥2× lub gdy danie jest prep-ahead-friendly, z blokadą jeśli resztki na docelowy dzień już zaplanowane). Ręcznie zweryfikowane na emulatorze 2026-08-09: wygenerowanie całego tygodnia wypełniło wszystkie 35 slotów z sensownymi daniami; wyczyszczenie Poniedziałku wyzerowało tylko ten dzień (Wtorek bez zmian); przycisk „Ugotuj na 2 dni” po ustawieniu skali 2× na Poniedziałkowym Obiedzie (425→850 kcal, dokładnie ×2) poprawnie zaplanował „🍱 Kotlety…” na Środę tej samej kategorii; po wyczyszczeniu Soboty baner FR-24 sam pojawił się dla Obiadu („Faszerowana papryka… — ryżem” trafiło w słowo kluczowe), kliknięcie „Tak, powtórz” ustawiło resztki z bazową skalą (Razem: 320 kcal, zgodnie z kcal przepisu); „🔁” zmienił wylosowane danie na inne z tej samej kategorii. Zero crashy. **Uwaga do FR-22**: w `index.html` istnieje tylko czyszczenie pojedynczego dnia (`data-day-clear`) — nie znaleziono osobnego przycisku „wyczyść cały tydzień” w kodzie źródłowym mimo tytułu FR, więc port jest jeden-do-jednego z tym, co faktycznie robi web, nie z samym tytułem wymagania.
- **FR-25/FR-27** (lista zakupów ze składników przepisów, całego dnia/tygodnia): `ShoppingItem` przebudowany na kształt `state.shopping[canon+"|"+unitCat]` z `index.html` (pole `contributions: Map<źródło, ilość>` zamiast starego wolnego pola `unit`). `ShoppingOperations.addRecipe`/`removeRecipe`/`addSingleIngredient`/`isRecipeAdded`/`addDayPlan`/`addWeekPlan` w `logic/` — jeden wspólny rdzeń (`addContribution`/`removeContribution`) używany zarówno przez dodanie całego przepisu (FR-25), pojedynczego składnika (FR-16, przez syntetyczny klucz `single:<id>:<canon>`), jak i cały dzień/tydzień z Planera (FR-27, z odjęciem tego samego dedupu `if(recipeAdded) skip` co `index.html`, więc to samo danie zaplanowane dwa razy w tygodniu dolicza się tylko raz). `ShoppingDisplay` w `logic/` — port tabeli `CANON_GENITIVE` (155 pozycji, przepisane bez zmian) + `shopDisplayName`/`formatQty`, z odmianą liczebnikową (FR-29) dla `unitCat==="count"` i dopełniaczem dla reszty. `PlannerOperations.scaleIngredientText`/`scaleIngredients` — port `scaleIngredientText`/`formatScaledAmount` z `index.html` (skalowanie wiodącej ilości w tekście składnika przy FR-20). `RecipeListScreen.kt`: nowy przycisk „🛒 Dodaj do listy zakupów” / „✓ Na liście zakupów” (toggle) na karcie przepisu. `ShoppingScreen.kt`: usunięto placeholder ręcznego dodawania dowolnej pozycji (nie odpowiadał żadnej realnej funkcji z `index.html` — pusty stan listy w wersji web wprost mówi, że jedyne wejścia to przepisy/Planer) i dodano przycisk „🛒 Dodaj składniki z całego tygodnia” (FR-27). `PlannerScreen.kt`: dodano przycisk „🛒 Dodaj składniki z tego dnia” per karta dnia. Ręcznie zweryfikowane na emulatorze 2026-08-09: dodanie Szakszuki (6 składników) + Omletu (7 składników, współdzieli „jajka” i „oliwę”) dało 11 pozycji z poprawnie zsumowanymi ilościami („4 jajka”, „2 oliwa z oliwek”); usunięcie Omletu wróciło dokładnie do 6 pozycji z oryginalnymi ilościami („2 jajka”, „1 oliwa z oliwek”), reszta się nie zmieniła; wygenerowanie całego tygodnia + „Dodaj składniki z całego tygodnia” dało 65 pozycji bez crasha; ponowne kliknięcie przycisku dnia dla dnia już w całości dodanego zostało poprawnie zignorowane (licznik pozostał 65, bez duplikatów). Zero crashy.
- **FR-50** (redukcja animacji): `isReducedMotionEnabled(context)` w `ui/ReducedMotion.kt` — sprawdza systemowe ustawienie dewelopera "Skala animatora" (`Settings.Global.ANIMATOR_DURATION_SCALE`), androidowy odpowiednik `prefers-reduced-motion`. Jedyna dotąd autorska animacja w aplikacji (`AnimatedVisibility` FAB-a powrotu do góry, FR-5) używa `EnterTransition.None`/`ExitTransition.None` gdy włączone. Dialogi Compose (`Dialog(...)`) mają domyślne animacje systemowego okna, które i tak respektują tę samą skalę na poziomie OS, więc nie wymagały osobnej obsługi. Ręcznie zweryfikowane na emulatorze 2026-08-09: `adb shell settings put global animator_duration_scale 0` + przewinięcie listy przepisów — FAB pojawia się poprawnie (bez crasha) zarówno przy włączonej, jak i wyłączonej redukcji animacji.
- **FR-46** (Wstecz nie zamyka od razu aplikacji): zero nowego kodu — zachowanie wynika za darmo z istniejącej architektury. Compose'owy `Dialog`/`AlertDialog` domyślnie zamyka się na systemowy Wstecz (`dismissOnBackPress=true` we wszystkich okienkach modalnych w aplikacji — CookHistoryDialog, PantryCheckDialog, PlanPickerDialog, PlannerSlotDialog, MacroInfoDialog itd.), a każdy z nich żyje we własnym Window, nie na stosie `NavController`, więc nie ma czego "zjeść" pustym Wstecz po zamknięciu inną drogą (przycisk ✕, dotknięcie tła) — całe drugie kryterium akceptacji jest strukturalnie niemożliwe do naruszenia w tej architekturze, nie tylko "naprawione". Nawigacja dolnym paskiem (`popUpTo(startDestination){saveState=true}` + `launchSingleTop`/`restoreState`) sama w sobie daje dokładnie żądane zachowanie: Wstecz z zakładki innej niż Przepisy wraca do Przepisy (start destination), a Wstecz z Przepisy (root, pusty stos) poprawnie kończy aplikację — standardowe zachowanie `ComponentActivity`, nic dodatkowego do zaimplementowania. Ręcznie zweryfikowane na emulatorze 2026-08-10 (`adb shell input keyevent 4` + `dumpsys activity activities`): (1) otwarte okienko „Zrobione” zamyka się na Wstecz, aplikacja zostaje na ekranie Przepisy; (2) Wstecz na zakładce Zakupy wraca do Przepisy, `topResumedActivity` nadal wskazuje `MainActivity`; (3) kolejne Wstecz na Przepisy poprawnie kończy aplikację (`topResumedActivity` przełącza się na launcher); (4) zamknięcie okienka przyciskiem „✕” (nie systemowym Wstecz), a potem JEDNO naciśnięcie Wstecz — od razu kończy aplikację, bez pustego "zjedzonego" wpisu. Punkt kryterium o skanerze kodów kreskowych (FR-31) nie dotyczy — FR-31 nie jest jeszcze ported.
- **FR-55/57** (ocenianie przesunięciem, trwałe oznaczenie, ranking): `RecipeRating`/`RecipeRatingOperations` w `logic/` (testy JUnit — `setRating`/`clearRating`/`sortByRating`: lubiane→nieocenione→nielubiane, stabilne wewnątrz grupy). Karta przepisu owinięta w `Box` z `Card` reagującą na `detectHorizontalDragGestures` (oś pozioma, próg 90dp — `Animatable` do płynnego podążania za palcem i animowanego powrotu po puszczeniu); przekroczenie progu w prawo/lewo ustawia lubię/nie lubię, poniżej progu wraca do zera bez zapisu. Oceniona karta dostaje kolorową obwódkę (`Modifier.border`, zielona/czerwona) i małą plakietkę 👍/👎 w rogu, klikalną by skasować ocenę — karta NIGDY nie znika z listy. Przełącznik „❤️ Ranking” w pasku filtrów (obok „🎯 Dopasowanie”) sortuje niezależnie, w kolejności zastosowania zgodnej z `index.html` (najpierw dopasowanie, potem ranking, jeśli oba włączone). Ręcznie zweryfikowane na emulatorze 2026-08-10: przesunięcie w prawo dało zieloną obwódkę + 👍 na jednej karcie, przesunięcie w lewo na innej dało czerwoną obwódkę + 👎, dotknięcie plakietki skasowało ocenę, zwykłe przewijanie listy w pionie działało bez przeszkód (gest poziomy nie kolidował), a włączenie „❤️ Ranking” przeniosło polubioną kartę na sam początek listy po przewinięciu do góry. Zero crashy. **FR-56** (balonowy napis „❤️ LUBIĘ”/„👎 NIE LUBIĘ” podczas przeciągania): dodany od razu jako naturalne rozszerzenie tego samego gestu — `Text` wyśrodkowany na karcie, `alpha` narastające z `abs(offsetX)/próg` (0 w spoczynku, 1 tuż przed zatwierdzeniem). Kompiluje się i korzysta z tego samego stanu `offsetX`, który już zweryfikowano jako poprawny (obwódka/plakietka po puszczeniu są dokładnie tym, co powinno wynikać z tej samej wartości w trakcie przeciągania) — ale samego przejściowego napisu NIE udało się złapać na zrzucie ekranu: gest `adb shell input swipe` na tym emulatorze kończy się (łącznie z podniesieniem palca) szybciej niż da się zrobić zrzut ekranu w trakcie, niezależnie od podanego czasu trwania. Status ⏳ zamiast ✅ właśnie z tego powodu — kod jest oceniany jako poprawny przez konstrukcję, ale nie ma bezpośredniego dowodu wizualnego jak przy innych FR-ach w tym pliku.
- **FR-59** (wyśrodkowane okienka, pełna szerokość): większość kryteriów była już spełniona za darmo dzięki architekturze — Compose'owy `Dialog` domyślnie centruje treść (nigdy nie budowano żadnego "bottom sheet" w Androidzie, więc nie ma czego migrować), a `Card(Modifier.widthIn(max=480.dp))`/`fillMaxWidth().padding(16.dp)` już dawały szerokość zbliżoną do web'owego 600px+16px marginesu. Brakowało jednak jawnego `heightIn(max=560.dp)` w czterech okienkach (`CookHistoryDialog`, `PantryCheckDialog`, `PlanPickerDialog`, `MacroInfoDialog` — `PlannerSlotDialog` miało to już wcześniej) — dodano dla spójności i pewności, że długa treść przewija się wewnętrznie zamiast być nieosiągalna. Ręcznie zweryfikowane na emulatorze 2026-08-10 na przepisie z 8 składnikami („Sałatka z tuńczykiem, jajkiem na twardo i oliwkami”, `PantryCheckDialog`): bez przewinięcia widoczne było tylko 6 z 8 składników, ale „sok z cytryny” i „1 kromka chleba żytniego” były w pełni osiągalne po przewinięciu w dół wewnątrz okienka — potwierdzone `uiautomator dump` przed i po przewinięciu, nie tylko wizualnie. Zamykanie (✕, tło, systemowe Wstecz) działa tak samo jak w FR-46, bez zmian. Zero crashy.
- **FR-16** (sprawdzenie stanu spiżarni per przepis): nowy przycisk „🏺 Sprawdź stan spiżarni dla tego dania” (`OutlinedButton` z wypełnionym tłem `surfaceVariant`, pełna szerokość, min. wysokość 38dp — spełnia zrewidowane kryterium „nie sam tekst”) w rozwiniętej karcie przepisu, otwiera okienko-Card z jednym wierszem na składnik: status („🏺 opis” / „Brak w spiżarni”), duży przycisk „Mam to”/„✓ Mam” (min. 34dp) i „🛒” do dodania pojedynczego składnika do listy zakupów. `PantryOperations.toggleHaveIngredient`/`categoryForCanon` w `logic/` (testy JUnit) — port `openPantryModal`'s haveBtn handler z `index.html`, ograniczony do kategorii dostępnych w uproszczonym 7-kategoriowym modelu spiżarni Androida. `ShoppingViewModel` przeniesiony na poziom `MainActivity` (jak `PantryViewModel` wcześniej), żeby dodanie z tego okienka od razu było widoczne na zakładce Zakupy. Przycisk „📅 Zaplanuj to danie” z web'owej wersji NIE jest ported — nie ma jeszcze ekranu Planera (FR-18/19). Ręcznie zweryfikowane na emulatorze 2026-08-09 (przez `uiautomator dump`, nie tylko zrzuty ekranu): okienko listuje wszystkie składniki z „Brak w spiżarni”, tapnięcie „Mam to” na „150g piersi z kurczaka” natychmiast pokazuje „🏺 100 g” w oknie ORAZ nowy kafelek „kurczak (pierś)” / „Mięso i ryby” / „100.0 g” na zakładce Spiżarnia (współdzielony `PantryViewModel`), przycisk-wyzwalacz ma widoczne obramowanie+wypełnienie na zrzucie ekranu. Zero crashy.
- **FR-29** (odmiana gramatyczna nazw w spiżarni): `PantryDisplay` w `logic/` — port tabeli `PANTRY_PLURAL_FORMS` (32 pozycje, przepisane bez zmian z `index.html`) + `polishPluralCategory`/`pantryDisplayName`, z testami JUnit pokrywającymi wyjątek 12–14 (np. „13 jajek”, nie „jajka”). `PantryScreen.kt`: kafelek produktu z jednostką „szt.”/„szt” pokazuje odmienioną nazwę zamiast surowej (a obok liczbę bez jednostki, jak w wersji web), przeliczaną na bieżąco z `item.quantity`. Ręcznie zweryfikowane na emulatorze 2026-08-09 (przez `uiautomator dump`): dodanie „jajka” z ilością 1 → „jajko”, +3 → „jajka” (4 szt.), +9 więcej → „jajek” (13 szt., poprawnie łapie wyjątek 12–14 zamiast błędnie pokazać „jajka”). Zero crashy.
- **FR-12** (modal "Skąd te liczby?"): `Recipe.calc`/`RecipeCalcItem` (nowe pole, dane już były w `assets/recipes.json`, tylko nieużywane) + `RecipeRepository` sparsowany o tablicę `calc`. Rozwinięta karta przepisu pokazuje teraz linię B/W/T/Błonnik/IG/ŁG z przyciskiem "ℹ️", który otwiera dialog z rozbiciem na składniki (baza + wkład), notatką o kalibracji sumy do `kcal` przepisu i domyślnie zwiniętą legendą metodologii (jeden-do-jednego tekst z `index.html`). Ręcznie zweryfikowane na emulatorze na przepisie "Szakszuka…": rozbicie składników zgodne z danymi źródłowymi, suma (327 kcal) i notatka kalibracyjna poprawne, legenda rozwija się na dotknięcie, zero crashy. Mikroskładniki (Wapń/Wit. D/B12, `computeMicronutrients`) NIE są jeszcze pokazane — to osobne, jeszcze nierozpoczęte FR-64.

- **FR-58/FR-62** (pasek 7 kart dni na Zakupach): `ShoppingDayStrip` w `logic/` (testy JUnit) — port `renderShopDayStrip`/`addDayToShoppingList` z `index.html`: `buildCards` liczy dla każdego z 7 dni Planera `planned`/`onList` (wg `ShoppingOperations.isRecipeAdded`, więc to samo danie zaplanowane w dwóch dniach liczy się jako "na liście" w obu, gdy dodane z któregokolwiek), etykietę „Dziś”/„Jutro”/„Pojutrze” względem `todayIndex` (dziś liczone z rzeczywistej daty systemowej, `Calendar.DAY_OF_WEEK`) i stan „—”/„Dodaj”/„X/Y”/„Gotowe" z procentem paska postępu. `ShoppingOperations.addDayPlanWithSummary` (nowy wariant `addDayPlan` z FR-27, zwracający też liczby `added`/`already`) zasila `ShoppingDayStrip.addResultMessage`, który odtwarza dokładnie te same cztery warianty komunikatu co web'owy toast. `ShoppingScreen.kt`: `LazyRow` siedmiu kart (`combinedClickable` — zwykły dotyk dodaje i pokazuje `Toast` z komunikatem, długie przytrzymanie pokazuje `Toast` ze szczegółowym opisem "X/Y dań na liście zakupów", zgodnie z kryterium akceptacji o dostępności liczbowego opisu bez stałego wyświetlania), dzisiejsza karta ma pogrubioną obwódkę (`primary`), stan „Gotowe” dostaje wypełnienie `primaryContainer`, pusty dzień jest wyszarzony (`alpha 0.55`). Zastępuje wyłącznie brakujący dotąd w Androidzie odpowiednik web'owego paska — istniejący od FR-27 przycisk „🛒 Dodaj składniki z tego dnia” w `PlannerScreen.kt` (zakładka Planer, nie Zakupy) zostaje bez zmian, to inny, równoległy punkt wejścia do tej samej logiki. Ręcznie zweryfikowane na emulatorze 2026-08-10: po „🎲 Wygeneruj losowo cały tydzień” wszystkie karty pokazały „Dodaj”, dotknięcie karty „Dziś” dodało 5 dań (23 pozycje na liście) i zmieniło jej stan na „Gotowe” z pełnym paskiem oraz pokazało toast „Dodano składniki z 5 dań na dziś”; ponowne dotknięcie tej samej karty nie zmieniło liczby pozycji i pokazało „Dania na dziś są już na liście” (idempotentne); karta „Nie” (Niedziela) pokazała „1/5” mimo że nigdy jej nie dotknięto — bo jedno z jej dań było tym samym przepisem co w „Dziś” (poprawny, zamierzony efekt globalnego dedupu po recipe id, zgodny z web); poziomy scroll paska (`LazyRow`) faktycznie przewinął do kart „Nie”/„Sob”; długie przytrzymanie karty „Piątek” pokazało toast „Piątek: 0/5 dań na liście zakupów”. Zero crashy.

- **FR-65** (nazwa użytkownika w nagłówku): `ProfileViewModel._displayName` — osobny `StateFlow<String>` obok `_profile`, celowo nietykany przez `resetToDefault()` (kryterium: reset profilu diety nie może kasować nazwy). Nowa karta „👤 Konto” w `SettingsScreen.kt` (przed kartą „Twój profil”, bo Android nie ma jeszcze zakładek Ustawień z FR-71) z jednym `OutlinedTextField` (limit 30 znaków, zapis na każde naciśnięcie klawisza, bez przycisku „Zapisz”, etykieta/placeholder jeden-do-jednego z `index.html`). `MainActivity.kt`: prefiks `"$displayName · "` doklejony do napisu nagłówka w obu gałęziach (skonfigurowany/nieskonfigurowany profil), dokładnie jak `renderHeader`'s `namePrefix`. Status pierwotnie ⏳ z dwóch powodów: (1) nazwa była tylko w pamięci, nie przetrwałaby odświeżenia; (2) brak jeszcze zakładek Ustawień z FR-71. Ręcznie zweryfikowane na emulatorze 2026-08-10: wpisanie „Przemek” w polu Konto natychmiast (bez dodatkowego zapisu) zmieniło nagłówek na „Przemek · 👋 Ustaw swój profil…”, zero crashy. **AKTUALIZACJA 2026-08-10**: FR-71 (zakładki Ustawień) zaimplementowane tego samego dnia — karta „👤 Konto” żyje teraz w dedykowanej zakładce „Konto”; trwały zapis dodany (patrz notatka „Trwały zapis lokalny”), `displayName` przetrwa teraz restart. Oba powody nieaktualne, status podniesiony do ✅.

- **FR-61** (styl oceniania kart przesunięciem): nowy `SwipeRatingStyleViewModel` (`ui/`, wzorem `UiScaleViewModel`) — `StateFlow<SwipeRatingStyle>` (`BALLOON`/`GLOW`, domyślnie `BALLOON` jak `state.swipeRatingStyle||"balloon"` w `index.html`), współdzielony na poziomie `MainActivity` między kartą „🎈 Styl oceniania kart przesunięciem” w Ustawieniach (dwuopcyjny przełącznik tekstowy, jak istniejące Płeć w karcie profilu) a kartami przepisów. `RecipeCard` w `RecipeListScreen.kt`: `dragBorderColor` liczony tylko gdy trwa przeciąganie (`offsetX.value != 0f`) i styl to `GLOW` — ta sama formuła alfy `0.3+0.6*intensity` co `index.html`'s `useGlow` (`intensity = min(1, |offsetX|/próg)`), zielony/czerwony wg kierunku; poza przeciąganiem zawsze wraca do dotychczasowej stałej obwódki wg trwałej oceny (FR-57). W stylu `BALLOON` obwódka podczas przeciągania w ogóle się nie zmienia — sam balonowy napis (FR-56) niesie informację zwrotną, dokładnie jak w wersji web. Status ⏳ (nie ⏳→✅) z tego samego powodu co FR-56: przełącznik w Ustawieniach ręcznie zweryfikowany na emulatorze 2026-08-10 (domyślnie zaznaczone „Balonowa czcionka”, karta renderuje się poprawnie), ale sam kolor obwódki w trakcie przeciągania nie do złapania zrzutem ekranu przez `adb shell input swipe` z tych samych powodów co FR-56 — kod uznany za poprawny przez konstrukcję (ta sama, już zweryfikowana zmienna `offsetX` co FR-56/FR-57).

- **FR-64** (mikroskładniki w okienku wyliczeń): `Micronutrients` w `logic/` (testy JUnit) — port `MICRO_TABLE`/`computeMicronutrients` z `index.html`, kluczowany przez `IngredientCanon.thumbCanon(item.label)` (ta sama funkcja co FR-4), skalowany przez `item.qty` dokładnie jak `baseKcal`. Zwraca `null` (nie zera) gdy przepis nie ma żadnego rozpoznanego składnika. `MacroInfoDialog` w `RecipeListScreen.kt`: trzy plakietki 🦴/☀️/🥩 pod notatką kalibracyjną, w `FlowRow` (nie zwykły `Row` — trzy plakietki obok siebie nie mieszczą się w 480dp okienku, `Row` przycinałby/wypychał trzecią poza kartę, jak przy analogicznym buggu z `PlanPickerDialog`, FR-18/19/20); zastrzeżenie o orientacyjności (`Micronutrients.APPROX_NOTE`) dopisane raz do zwiniętej domyślnie legendy, nie powtarzane przy plakietkach. Ręcznie zweryfikowane na emulatorze 2026-08-10 na „Szakszuka ze szpinakiem i pomidorami”: plakietki pokazały „🦴 Wapń ~92 mg”, „☀️ Wit. D ~2.2 µg”, „🥩 B12 ~0.9 µg” (2 jajka + szpinak, reszta składników pominięta jak oczekiwano), poprawnie zawinięte w drugi wiersz zamiast wychodzić poza kartę po naprawie `Row`→`FlowRow`. Zero crashy.

- **FR-70** (kropelki nawodnienia w nagłówku): `WaterOperations.tapDroplet` w `logic/` (testy JUnit) — jeden-do-jednego port `renderHeaderWater`'s `state.water.count = (count===i+1)?i:i+1` z `index.html`: kliknięcie kropelki `i` ustawia licznik na `i+1`, chyba że licznik już tam jest — wtedy cofa o jedną szklankę, bez zawijania. Nowy `WaterViewModel` (`ui/`, lokalny stan jak reszta), pasek 8 kropelek (💧/⚪ + „N/8") wstawiony do `Column` tytułu `TopAppBar` w `MainActivity.kt` — zawsze widoczny na każdej zakładce (Android nie ma jeszcze zwijanego nagłówka z FR-44/45, więc kryterium „niezależnie od tego czy nagłówek jest zwinięty” jest spełnione trywialnie: nagłówek nigdy się nie zwija). Status pierwotnie ⏳ z dwóch powodów: (1) lokalny stan bez trwałego zapisu; (2) zakładka Postęp to placeholder, więc "nagłówek↔Postęp" nie miało drugiej strony do zsynchronizowania. Ręcznie zweryfikowane na emulatorze 2026-08-10: pasek renderuje się na zakładce Przepisy z „0/8”, kliknięcie kropelki ustawiło „3/8” z trzema wypełnionymi 💧, powtórne kliknięcie DOKŁADNIE tej samej kropelki cofnęło do „2/8” (nie wyzerowało) — czyli dokładnie zachowanie, które ta rewizja FR-70 miała naprawić względem pierwotnego jednoprzyciskowego licznika. **AKTUALIZACJA 2026-08-10**: oba powody usunięte tego samego dnia — zakładka Postęp dostała pełny widok wody dzielący TEN SAM `WaterViewModel` (patrz notatka FR-60/FR-37/FR-40, potwierdzone zrzutem ekranu że kropelka dotknięta w Postępie natychmiast aktualizuje pasek w nagłówku i odwrotnie), a trwały zapis dodany (patrz notatka „Trwały zapis lokalny"). Status podniesiony do ✅. Zero crashy.

- **FR-72** (wymuszenie ustawienia profilu przy pierwszym uruchomieniu): większość kryteriów była już spełniona za darmo dzięki temu, jak FR-6 zbudowano od początku (`Profile(configured = false)` domyślnie, puste pola w formularzu, zaproszenie w nagłówku i pod formularzem, `resetToDefault()` ustawiające `configured = true`) — brakowało tylko jednego: znaczek 🎯 na kartach przepisów liczył się zawsze z wewnętrznych wartości domyślnych profilu, nawet przed jego skonfigurowaniem, zamiast być całkiem ukryty. Naprawione w `RecipeListScreen.kt` (`matchScores` = `emptyMap()` dopóki `!profile.configured`) — jeden-do-jednego port `state.profile.configured ? recipeMatchScore(...) : null` z `index.html`. Świadomie NIE zmieniono `PlannerSlotDialog` w `PlannerScreen.kt` (wynik 🎯 przy wyborze dania w Planerze) — sam `index.html` też nie bramkuje tego miejsca `configured` (`openPlannerPickerModal` woła `recipeMatchScore` wprost), więc to zgodne z web, nie przeoczenie. Ręcznie zweryfikowane na emulatorze 2026-08-10: po odinstalowaniu i świeżej instalacji APK żadna karta na liście Przepisów nie pokazywała już znaczka 🎯 (wcześniej, przed poprawką, pokazywały np. „73%”/„25%” mimo nieskonfigurowanego profilu). Zero crashy.

- **FR-36** (pierścień kalorii + lista posiłków + zjedzone w nagłówku): `EatenEntry`/`EatenOperations` w `logic/` (testy JUnit) — port `state.eaten[date][cat]`/`setEaten`/`isEaten`/`dailyEatenKcal` z `index.html`, celowo ograniczony do „dziś" (Android nie ma jeszcze historii po dowolnej dacie — to FR-41/42, nieported) i bez snacków (`state.eaten[date].snacks`, to osobne FR-33/34, też nieported). Nagłówek: `CircularProgressIndicator` (pierścień) + wartość = `PlannerOperations.dayTotalKcal` na dziś (**zaplanowane**, NIE zjedzone kcal — jeden-do-jednego z `index.html`'s `renderHeader`, gdzie `kcalRingValue`/`pct` liczą się z `todaysPlannedKcal()`, całkiem niezależnie od `state.eaten`; oznaczenie posiłku jako zjedzonego zmienia tylko wiersz i linijkę „Zjedzone/Zostało" pod spodem, NIE sam pierścień — zaskakujące, ale to realne zachowanie źródła, nie błąd portu), 5 wierszy slotów Planera z `detectHorizontalDragGestures` (prawo-only, próg zatwierdzenia ~22dp z maks. 40dp, proporcjonalnie do `SWIPE_COMMIT`/`SWIPE_MAX` z web) przełączających `EatenViewModel.toggle`, i linijka „🍽️ Zjedzone: X kcal · Zostało: Y kcal" z `EatenOperations.dailyEatenKcal`. Cały panel manualnie zwijany/rozwijany dotknięciem tytułu „Dieta App” (`headerExpanded`, domyślnie rozwinięty na zakładce Przepisy, zwinięty na pozostałych, resetowany przy każdej zmianie zakładki) — baza pod przyszłe FR-44/45 (automatyczne chowanie na przewijanie + pierwszeństwo ręcznego przełącznika), ale bez samej logiki scroll-based, której tu świadomie nie ma. **Napotkany i naprawiony bug**: pierwsza wersja umieszczała cały panel wewnątrz `TopAppBar`'s `title = {...}` — Material3 (compose-bom 2024.10.00) narzuca tam twardy limit wysokości i po prostu WYCINA nadmiarową treść bez żadnego wyjątku/loga (potwierdzone: nawet zwykły, zawsze-komponowany `Text("DEBUG…")` wstawiony tam nigdy się nie pojawiał, mimo braku crasha) — naprawione przeniesieniem paska wody i panelu poza `title`, jako rodzeństwo `TopAppBar` wewnątrz jednej, wspólnie teal-tłowej `Column` w slocie `topBar` samego `Scaffold`, który takiego ograniczenia nie ma. Ręcznie zweryfikowane na emulatorze 2026-08-10: świeża instalacja pokazała pierścień „0 z 1480 kcal” i pustą listę slotów; po „🎲 Wygeneruj losowo cały tydzień” pierścień pokazał „1500 z 1480 kcal” z nazwami dań przy każdym slocie; przesunięcie w prawo wiersza „Śniadanie” dodało zielony ✓, przekreśliło nazwę dania i zmieniło linijkę na „Zjedzone: 350 kcal · Zostało: 1130 kcal” (pierścień bez zmian, zgodnie z opisanym wyżej zachowaniem źródła); ponowne przesunięcie tego samego wiersza cofnęło oznaczenie; dotknięcie tytułu zwinęło cały panel (chevron ⌃→⌄), zostawiając tylko pasek wody widoczny. Zero crashy.

- **FR-36/v2 (przeprojektowanie na podwójny pierścień, 2026-08-11)**: naprawiono rozbieżność opisaną w notatce wyżej — na wyraźne życzenie użytkownika (dopasowanie do referencyjnych zrzutów ekranu nowego design systemu) pierścień w `HeaderKcalPanel` przestał liczyć się z `PlannerOperations.dayTotalKcal` (zaplanowane) i liczy się teraz z `EatenOperations.dailyEatenKcal(eatenEntries) + EatenOperations.snacksKcal(snacks)` (faktycznie zjedzone), dokładnie jak `index.html`'s przepisany `renderHeader()` (`eaten`/`dailyEatenKcal(today)` zamiast `todaysPlannedKcal()`) — teraz kod W KOŃCU zgadza się z tym, co `FR-36.md` opisywało od v1. Do tego dodano DRUGI, wewnętrzny łuk `CircularProgressIndicator` (`Modifier.fillMaxSize().padding(11.dp)`, mniejszy promień) pokazujący `waterCount/WaterOperations.MAX_LEVEL` — jeden wspólny pierścień, zewnętrzny pomarańczowy łuk (`0xFFF5822B`, `--ring-kcal` w web) = kalorie, wewnętrzny niebieski łuk (`0xFF3E8EF5`, `--ring-water` w web) = nawodnienie, oba animowane przez Compose'ową domyślną animację `CircularProgressIndicator` (web ma analogiczny `transition: stroke-dasharray .5s ease` na obu okręgach SVG). Po lewej stronie pierścienia nowa kolumna „POZOSTAŁO” / wartość / „kcal” (`remaining = max(0, targetKcal - eatenKcal)`), jeden-do-jednego z web'owego `.kcal-remaining`/`#kcalRemainingValue`. Kropelki 💧/⚪ w `HeaderWaterRow` zastąpiono nowym `WaterCupIcon` — rysowany `Canvas` (zaokrąglony prostokątny korpus kubka + uchwyt jako łuk), bez zależności od dodatkowych zasobów wektorowych, ten sam duch co `WeightChart`/`KcalHistoryChart` (żadnych nowych zależności Gradle); web dostał analogiczny `cupIconSvg()` (inline SVG, dzielony między pasek nagłówka a rząd kubków w zakładce Postęp). Ręcznie zweryfikowane na emulatorze 2026-08-11 w DWÓCH motywach (ciemny i jasny — zrzuty ekranu `uiautomator dump` + screenshoty): świeży stan pokazał pierścień „0 z 1480 kcal” z pełnym niebieskim wewnętrznym łukiem (nawodnienie już na 8/8 z wcześniejszej sesji) i „POZOSTAŁO 1480 kcal” po lewej, lista slotów Planera i linijka „Zjedzone: 0 kcal · Zostało: 1480 kcal” bez zmian względem poprzedniej wersji. Zero crashy w obu motywach.

- **FR-33/FR-34** (globalny „➕” + baza 336 produktów): `SnackNutritionDb` w `logic/` — wygenerowana skryptem (Node, nie przepisana ręcznie) z `SNACK_NUTRITION_DB` w `index.html` (336 pozycji, jeden-do-jednego, zero rozbieżności — potwierdzone testem liczącym wpisy), z `estimate(text)` będącym portem `estimateSnackKcal`: parsuje tekst przez `RecipePantryMatching.parseIngredient` (ta sama funkcja co FR-16/FR-25) i skaluje kaloryczność jak `baseKcal`. Nowy `EatenViewModel.snacks`/`addSnack`/`removeSnack` (+ `EatenOperations.snacksKcal`, testy JUnit) doklejają się do istniejącego FR-36 „Zjedzone” — ta sama linijka podsumowania liczy teraz sumę kategorii Planera ORAZ przekąsek. `QuickAddSnackDialog` w `MainActivity.kt`: pole nazwy z listą podpowiedzi (startsWith, potem contains, maks. 8, od 2 znaków — jeden-do-jednego `renderSuggestions`), auto-wypełniane pole kcal (edytowalne, `kcalWasAutoFilled` flaga jak w web — ręczna zmiana kcal wyłącza auto-uzupełnianie), przycisk „+ Dodaj”. Przycisk „➕” w `TopAppBar`'s `actions` (obok ⚙️), widoczny na każdej zakładce. **Dokończone 2026-08-10**: pływający przycisk „➕” na zakładce Postęp (v2 wymagania) był świadomie odłożony dopóki ta zakładka była placeholderem — teraz, po FR-37/40/41/42/60, dodany jako `Scaffold`'s `floatingActionButton` w `MainActivity.kt`, renderowany warunkowo (`if (currentRoute == Screen.Progress.route)`), wołający TĘ SAMĄ `showQuickAddDialog = true`/`QuickAddSnackDialog` co przycisk w nagłówku — jedna implementacja, dwa wejścia, zgodnie z trzecim kryterium akceptacji. Ręcznie zweryfikowane na emulatorze 2026-08-10: przycisk widoczny w prawym dolnym rogu WYŁĄCZNIE na zakładce Postęp, dotknięcie otworzyło dokładnie to samo okienko „➕ Dodaj przekąskę lub dodatkowe danie” co przycisk w nagłówku, przełączenie na zakładkę Przepisy natychmiast go ukryło. `versionCode` 33→34, `versionName` 0.1.32→0.1.33. **Napotkany i naprawiony bug podczas portu bazy**: mój własny test „każda pozycja bazy rozpoznawalna po wpisaniu jej dosłownej nazwy” złapał realną, przedtem niezauważoną lukę w `IngredientCanon.RAW_TO_CANON` — 5 brakujących aliasów (`borówki`/`jagody`→`borówki / jagody`, `sałata`→`sałata / mix sałat`, `pestki dyni`/`pestki słonecznika`→`pestki (dyni/słonecznika)`) obecnych w `index.html`, ale pominiętych przy wcześniejszym porcie FR-4/FR-29 — dodane teraz, więc naprawia to też starsze thumbnaile/dopasowanie spiżarni dla tych składników, nie tylko przekąski. Pozostałe 9 pozycji bazy (warianty suszone/„w oleju”/„w rożku" + dwie już-shadowowane pozycje `pestki dyni`/`pestki słonecznika`) są świadomie nieosiągalne przez wpisanie — to nie regresja portu, tylko właściwość współdzielonej tabeli `UNIT_WORDS`/`RAW_TO_CANON`, identyczna w `index.html` (potwierdzone bezpośrednim porównaniem obu źródeł), udokumentowana wprost w teście zamiast cicho pomijana. Ręcznie zweryfikowane na emulatorze 2026-08-10: wpisanie „banan” pokazało podpowiedzi („banan”, „chipsy bananowe”) i auto-uzupełniło „105” kcal, „+ Dodaj” dodało wiersz „🍿 banan · 105 kcal” z przyciskiem ✕ do panelu nagłówka i zaktualizowało „Zjedzone: 105 kcal · Zostało: 1375 kcal”, usunięcie przyciskiem ✕ poprawnie wróciło do „Zjedzone: 0 kcal”. Zero crashy.

- **FR-38/FR-39 (powiadomienia i przypomnienie o piciu wody, 2026-08-11)**: natywna implementacja zamiast portu web'owych Service Worker/`Notification`/`postMessage` API (Android nie ma żadnego z nich) — nowy pakiet `notifications/`. `WaterReminderScheduling` w `logic/` (testy JUnit, `WaterReminderSchedulingTest.kt`, 7 przypadków) — jeden-do-jednego port `parseHM`/`isActiveMinute`/`computeNextReminderAt` z web (`sw.js`/`index.html`, oba miały identyczną kopię tej matematyki). `WaterNotificationStore` (`data/`, SharedPreferences, CELOWO osobny od `LocalStateStore`) trzyma stan czytany/pisany zarówno przez UI (`WaterNotificationViewModel`), jak i przez `WaterActionReceiver`/`WaterReminderAlarmReceiver` — dwa `BroadcastReceiver`y bez ViewModelu, więc nie mogły dzielić stanu przez zwykły `StateFlow`; ten sam podział co web'owa osobna `water-kv` IndexedDB. `WaterNotifications` buduje oba powiadomienia (`NotificationCompat`, kanały `water_tracker`/`water_reminder`) z akcjami kierowanymi do `WaterActionReceiver` przez `PendingIntent.getBroadcast` (nigdy nie otwierają Activity) — kolejność `+1`/`-1` NIE jest zamieniona jak w `sw.js` (tamten hack obchodził udokumentowanego buga Chrome-na-Androidzie przy web-push, którego naciwne `NotificationCompat` akcje nie mają). `AlarmScheduler` planuje JEDEN jednorazowy `AlarmManager.setAndAllowWhileIdle(RTC_WAKEUP, ...)` (celowo NIEdokładny, żeby nie wymagać osobnej zgody „Alarmy i przypomnienia” na API 31+ — akceptowalny kompromis, web'owa wersja i tak ma tylko 30s-owe pollowanie); `WaterReminderAlarmReceiver` sam siebie planuje ponownie po każdym odpaleniu ORAZ po `BOOT_COMPLETED` (alarmy `AlarmManager` nie przeżywają restartu urządzenia same z siebie). `WaterNotificationBridge` — prosty obiekt z nullable callbackami zamiast web'owego `postMessage` (Receiver i reszta appki żyją w tym samym procesie, więc zwykłe wywołanie funkcji wystarcza) — daje aktualizację licznika wody w UI NATYCHMIAST po dotknięciu `+1`/`-1` w powiadomieniu, bez czekania na `onResume` jak web musiał (`syncPendingWaterFromNotifications` na `visibilitychange`). Zakładka Ustawienia → Przypomnienia (dotąd placeholder z tekstem „jeszcze nie działają”) dostała trzy karty: toggle powiadomienia licznika, formularz przypomnienia (interwał/godziny aktywności/status następnego terminu) i log diagnostyczny ostatnich 20 zdarzeń (`renderWaterActionLog` port) — obie karty proszą o `POST_NOTIFICATIONS` (API 33+) przez `rememberLauncherForActivityResult`, dokładnie jak istniejący wzorzec logowania Google w tym samym pliku. **Zweryfikowane na emulatorze 2026-08-11 wprost przez systemowe usługi Androida (nie tylko przez UI)**: `adb shell dumpsys notification` potwierdził poprawnie złożone powiadomienie licznika („💧 Nawodnienie” / „💧💧💧💧💧💧💧💧  8 / 8 szklanek”, dwie akcje z `PendingIntent`ami celującymi we własny `WaterActionReceiver`); próba wysłania broadcastu do tego receivera z powłoki `adb` (uid powłoki, nie appki) została poprawnie ODRZUCONA przez system („is not exported from uid 10213”) — potwierdza to, że `android:exported="false"` faktycznie chroni odbiornik, więc jedynym sposobem na jego wywołanie jest własny `PendingIntent` appki, zgodnie z zamysłem. Włączenie przypomnienia w UI pokazało „Następne przypomnienie: jutro, 08:00” (bieżący czas był już po 22:00, więc poprawnie przesunięte na start okna aktywności następnego dnia) i `adb shell dumpsys alarm` potwierdził realny zaplanowany `RTC_WAKEUP` na `2026-08-11 08:00:00.000` z tagiem `*walarm*:com.przemas230.dietaapp.action.FIRE_REMINDER`; wyłączenie przypomnienia w UI natychmiast pokazało w `dumpsys alarm` `Reason=alarm_cancelled` i przycisk wrócił do stanu „🔔 Włącz…”. Wyłączenie powiadomienia licznika w UI potwierdzone brakiem jakiegokolwiek rekordu `pkg=com.przemas230.dietaapp` w kolejnym `dumpsys notification`. **Nie zweryfikowane fizycznym dotknięciem przycisku w rozwiniętym powiadomieniu** — na tym konkretnym obrazie AVD polecenia `adb shell cmd statusbar expand-notifications` i gesty `input swipe` z góry ekranu otwierały panel Szybkich ustawień (Internet/Bluetooth/Latarka/Nie przeszkadzać) zamiast lub oprócz właściwej listy powiadomień, mimo kilku prób różnymi gestami/poleceniami — potwierdzone brakiem jakiegokolwiek węzła `package="com.przemas230.dietaapp"` w zrzutach `uiautomator dump` tego panelu; to ograniczenie automatyzacji tego konkretnego emulatora, nie appki. Sama logika w `WaterActionReceiver.handleTrackerAction` (odczyt/zapis SharedPreferences, `coerceIn` do przedziału, dopisanie do logu JSON) używa dokładnie tych samych, już sprawdzonych wzorców co `LocalStateStore`/`AppUpdateViewModel` gdzie indziej w tym module. Zero crashy w całej turze weryfikacji.

- **Runda wizualnych weryfikacji (2026-08-10)**: kilka FR-ów było już zaimplementowanych i tylko czekało na potwierdzenie na emulatorze, bez zmian w kodzie. **FR-75** (kafelki listy zakupów): dotknięcie „🏺 Kafelki” na liście z 6 pozycjami pokazało siatkę 3×N z poprawnym emoji per produkt i czerwoną plakietką „−ilość” (spiżarnia pusta w teście, więc cała ilość brakująca — zgodny z oczekiwanym fallbackiem `missingAfterPantry`). **FR-74**: pigułka „🔍 Śniadania” widoczna na Przepisach, Planer pokazał 5 osobnych slotów (Śniadanie/II Śniadanie/Obiad/Kolacja/Deser) per dzień. **FR-32**: rozwinięcie karty przepisu pokazało ☆ przy każdym składniku; dotknięcie gwiazdki przy „2 jajka” i „garść szpinaku” zmieniło oba na ★ (pogrubiony zielony tekst); „💡 Pomysł na danie z ulubionych składników” z tymi dwoma ulubionymi wygenerował realny pomysł „Duszone jajka z szpinak i kaszą” ze stopką „Wygenerowane z Twoich ulubionych składników: jajka, szpinak”. **FR-55/FR-56/FR-57** (przesunięcie karty, styl domyślny „balloon”): powolne przesunięcie karty „Jednopatelniowy kurczak…” w prawo (symulowane `adb shell input swipe` z długim czasem trwania, zrzut ekranu zrobiony W TRAKCIE gestu) pokazało duży, czerwono-zielony napis „❤️ LUBIĘ” dokładnie tak jak opisuje FR-56; po zakończeniu przesunięcia karta trwale dostała zieloną obwódkę i plakietkę 👍 w rogu (FR-57's trwałe oznaczenie). **FR-61** zweryfikowane tylko dla domyślnego stylu `BALLOON` — drugi dostępny styl (`GLOW`) NIE został osobno przetestowany wizualnie w tej turze, tylko przez inspekcję kodu (ten sam mechanizm przełącznika co już zweryfikowany `SwipeRatingStyleCard`). Zero crashy w całej rundzie.

- **FR-35** (emotikonki przy rozpoznanych składnikach/przekąskach): `IngredientCanon.withEmoji(text, canon)` (testy JUnit) — jeden-do-jednego port `canonEmoji`/`withEmoji` z `index.html`, doklejający `" $emoji"` gdy `CANON_INFO[canon]` je ma, inaczej zwracający tekst bez zmian. Trzy miejsca doczepione, dokładnie te wskazane w kryteriach akceptacji: lista składników na rozwiniętej karcie przepisu (`RecipeCardBody`, kanon liczony przez `RecipePantryMatching.parseIngredient`), lista podpowiedzi w oknie szybkiego dodania (`QuickAddSnackDialog`, kanon = sama nazwa sugestii z `SnackNutritionDb.TABLE`) oraz zapisany wpis dziennika (emoji doklejane do nazwy w momencie `onAdd`, więc zapisana nazwa i późniejsze wyświetlenie w panelu nagłówka zawsze się zgadzają — nie trzeba dwa razy liczyć). Okienko „🏺 Sprawdź stan spiżarni” (FR-16) dostało to samo przy okazji, bo już liczyło `parsed.canonName` do innych celów w tym samym wierszu. Ręcznie zweryfikowane na emulatorze 2026-08-10: rozwinięta karta „Jednopatelniowy kurczak…” pokazała „150g piersi z kurczaka 🍗”, „35g suchej kaszy gryczanej niepalonej 🌾”, „1 łyżka oliwy z oliwek 🫒” itd.; w oknie szybkiego dodania wpisanie „jajk” pokazało podpowiedź „jajka” z emoji. Zero crashy.

- **FR-43** (pasek filtrów przyklejony pod nagłówkiem): zero nowego kodu — zachowanie wynika za darmo z architektury `RecipeListScreen.kt`, w tym samym duchu co FR-46. Pole wyszukiwania i pozioma `LazyRow` pigułek kategorii/filtrów żyją w zewnętrznej `Column` NAD `LazyColumn` listy przepisów (nie wewnątrz niej, np. jako nagłówek listy), a ta zewnętrzna `Column` nie ma własnego przewijania — więc `LazyColumn` przewija się w pozostałej przestrzeni PONIŻEJ paska, który przez to nigdy się nie rusza, bez potrzeby jakiegokolwiek odpowiednika CSS `position:sticky`/`ResizeObserver`. Drugie kryterium (brak widocznego paska przewijania na poziomej liście pigułek) jest też strukturalnie niemożliwe do naruszenia — natywny `LazyRow` na Androidzie nie rysuje paska przewijania w ogóle, w odróżnieniu od przeglądarki desktopowej, więc nie ma czego chować. Ręcznie zweryfikowane na emulatorze 2026-08-10: przewinięcie listy przepisów (do widoku „Indyk duszony…”/„Gulasz z czerwonej soczewicy…”) zostawiło pole wyszukiwania i pasek kategorii (z zaznaczonym „Obiady”) dokładnie na miejscu, bez nakładania się ani szpary, żadnego paska przewijania nie widać pod pigułkami.

- **FR-44/FR-45** (automatyczne chowanie nagłówka na przewijanie + pierwszeństwo ręcznego przełącznika): `HeaderScrollBehavior` w `logic/` (testy JUnit) — pure funkcje `scrolledDown`/`scrolledUp`/`isNearTop` operujące na parze `(firstVisibleItemIndex, firstVisibleItemScrollOffset)` zamiast pojedynczego `scrollY` jak w web (Compose's `LazyListState` nie eksponuje jednej wartości pikselowej), ale ten sam kierunkowy pomysł co `applyState`/`NEAR_TOP`/`DELTA` z `index.html`. `LazyListState` przeniesiony (hoisted) z `RecipeListWithScrollToTop` do `DietaAppRoot` w `MainActivity.kt` (nowy parametr `listState` w `RecipeListScreen`, domyślnie `rememberLazyListState()` dla wywołań spoza `MainActivity`), obserwowany przez `LaunchedEffect(recipeListState, currentRoute)` + `snapshotFlow` — działa tylko gdy `currentRoute == Screen.Recipes.route` (jak web). `headerAutoFrozen` (osobny stan od `headerExpanded`, oba keyed on `currentRoute` żeby resetować się przy każdym wejściu na zakładkę Przepisy — kryterium akceptacji FR-44) — ręczne zwinięcie (dotknięcie paska tytułu) ustawia `headerAutoFrozen = true` i blokuje automatykę dopóki użytkownik sam nie rozwinie (`headerAutoFrozen = false`) albo nie wyjdzie i wróci na zakładkę. FR-45's dodatkowe kryteria (ikony w rogu nigdy nie wyzwalają zwijania, otwarcie/zamknięcie modala nie cofa ręcznego zwinięcia) są strukturalnie spełnione przez architekturę: przyciski ⚙️/➕ żyją w osobnym slocie `actions` `TopAppBar`, nie wewnątrz klikalnej `Column` tytułu, a żaden Dialog w aplikacji nie dotyka `headerExpanded`/`headerAutoFrozen` w ogóle (Compose'owe `Dialog` nie ma odpowiednika przeglądanej w FR-45 usterki `history.back()`/`popstate` — to była specyfika architektury webowej, nieobecna tu w ogóle, patrz też FR-46). Ręcznie zweryfikowane na emulatorze 2026-08-10: przewinięcie listy przepisów w dół automatycznie zwinęło nagłówek (zniknął pierścień+lista slotów, chevron ⌄), przewinięcie z powrotem w górę automatycznie go rozwinęło (chevron ⌃); ręczne zwinięcie dotknięciem paska tytułu, a następnie przewinięcie w górę NIE rozwinęło nagłówka (zamrożenie działa); kolejne dotknięcie paska tytułu ręcznie rozwinęło nagłówek z powrotem. Zero crashy.

- **FR-71** (zakładki w Ustawieniach): `SettingsScreen.kt` przebudowany z jednej przewijanej listy kart na `enum SettingsTab`(KONTO/WYGLĄD/PRZYPOMNIENIA/ULUBIONE) + poziomy pasek `FilterChip` (ten sam styl co pigułki kategorii przepisów, zgodnie z opisem FR). „Twoja nazwa w aplikacji” (FR-65) przeniesione fizycznie na początek karty „⚙️ Twój profil” (usunięta osobna karta sprzed FR-71). Konto: `ProfileCard` + `FirebaseTestCard` (deweloperski test połączenia z Firebase, stoi tu tymczasowo zamiast nieportowanej jeszcze karty „☁️ Konto w chmurze” — patrz FR-69). Wygląd: `ThemeCard` + `UiScaleCard` + `SwipeRatingStyleCard` (wybór motywu kolorystycznego dodany później razem z FR-48, patrz notatka FR-48/FR-49/FR-63 niżej). Przypomnienia: dokończone 2026-08-11 razem z FR-38/39 (trzy karty — toggle powiadomienia, formularz przypomnienia, log diagnostyczny). Ulubione: dokończone 2026-08-11 nowym `FavoriteIngredientsCard` — pole wyszukiwania + `FlowRow` `FilterChip`ów dla KAŻDEGO nazwanego składnika ze wszystkich przepisów (`PantryTiles.buildTileNames`, ta sama funkcja co siatka Spiżarni), posortowanych `java.text.Collator` z polskim locale (żeby „ł”/„ż” itd. sortowały się tak jak `localeCompare('pl')` w web, nie wg surowego porządku Unicode), filtrowanych `contains` po wpisanym tekście — jeden-do-jednego port `renderFavIngChips`/`favIngSearch` z `index.html`, dzielący TEN SAM `FavoriteIngredientsViewModel` co ☆ na rozwiniętej karcie przepisu (FR-32), więc odznaczenie/zaznaczenie w jednym miejscu widać natychmiast w drugim. Ręcznie zweryfikowane na emulatorze 2026-08-11: lista pokazała się alfabetycznie posortowana (np. „bazylia” → „borówki / jagody” → „brokuł”, zgodnie z polskim sortowaniem); wpisanie „jajk” w wyszukiwarkę zawęziło listę do jednej pozycji „★ jajka” (już oznaczonej jako ulubiona z wcześniejszej sesji na karcie przepisu — potwierdza współdzielony stan); dotknięcie chipa zmieniło go z powrotem na „☆ jajka”. Zero crashy. `AppUpdateCard` (własny, androidowy sprawdzacz aktualizacji APK, bez odpowiednika w żadnej z 4 zakładek webowych — PWA aktualizuje się przez Service Worker, FR-53, N/D dla natywnej appki) świadomie NIE wpasowany na siłę w żadną zakładkę — zostawiony jako karta nad paskiem zakładek, zawsze widoczna. Kryterium „zmiana zakładek nie resetuje niezapisanych zmian w formularzu profilu” było realnym ryzykiem — naiwne `when(selectedTab)` usuwałoby `ProfileCard` z drzewa kompozycji przy każdej zmianie zakładki, kasując jego `remember`; naprawione przez `rememberSaveableStateHolder()` (ten sam mechanizm co zakładki `Pager`/`ViewPager`) + zmianę pól formularza profilu z `remember` na `rememberSaveable`. Status pierwotnie ⏳ (nie ✅) bo dwie z czterech zakładek były celowo puste — **AKTUALIZACJA 2026-08-11: obie (Przypomnienia, Ulubione) dostały realną zawartość, patrz wyżej — status podniesiony do ✅.** Ręcznie zweryfikowane na emulatorze 2026-08-10: 4 pigułki widoczne, kliknięcie „Wygląd” pokazało wyłącznie karty rozmiaru/stylu oceniania (ProfileCard zniknął), pole „Twoja nazwa w aplikacji” faktycznie na początku karty profilu; wpisanie „42” w pole Wiek, przełączenie na Wygląd i z powrotem na Konto pokazało nadal „42” w polu (niezapisana zmiana przetrwała przełączenie zakładki). Zero crashy.

- **FR-47** (brak FOUC domyślnych danych profilu): zero nowego kodu — usterka, której to wymaganie dotyczy, jest specyficzna dla architektury przeglądarki (HTML renderuje się z wartościami domyślnymi zanim JS zdąży podmienić je na zapisany stan z `localStorage`, dając widoczny na ułamek sekundy „błysk” złych danych) i strukturalnie nie ma jak wystąpić w tej aplikacji Compose: `ProfileViewModel` inicjalizuje `MutableStateFlow(Profile())` synchronicznie w konstruktorze, więc pierwsza faktyczna klatka złożona przez `DietaAppRoot` już czyta tę samą wartość, którą przeczyta każda kolejna — nie ma osobnego kroku „HTML z placeholderem, potem JS nadpisuje”, bo Compose nie ma odpowiednika DOM-u renderowanego przed hydratacją danych. To ten sam rodzaj argumentu co przy FR-43/FR-46 (własność architektury, nie coś świadomie zaimplementowanego). Nie pomylić z N/D (FR-51/52/53/54) — tam cały mechanizm (Service Worker, instalacja PWA) nie ma androidowego odpowiednika koncepcyjnie; tu odpowiadająca mu gwarancja użytkownika („brak migotania złych danych”) jak najbardziej dotyczy też natywnej appki, po prostu jest spełniona za darmo.

- **FR-75** (widok kafelkowy listy zakupów): `RecipePantryMatching.missingAfterPantry` w `logic/` (testy JUnit — niedobór, pełne/nadmiarowe pokrycie → `null`, konwersja jednostek spiżarni (np. „kg”→„g”), brak wpisu w spiżarni i niezgodna kategoria jednostki → pełna ilość jako brakująca) liczy ile z pozycji listy zakupów wciąż brakuje po odjęciu pasującego stanu spiżarni, tym samym ostrożnym fallbackiem co istniejący znacznik FR-16 „🏺 masz”. `ShoppingScreen.kt`: przełącznik `FilterChip` „📃 Lista” / „🏺 Kafelki (jak w spiżarni)” nad paskiem 7 kart dni (FR-58/62), oba widoki czytają/zapisują ten sam `ShoppingViewModel.items` (jedno źródło prawdy — zaznaczenie w jednym widoku widać natychmiast w drugim). Widok kafelkowy to `LazyVerticalGrid(columns = Fixed(3))` nowego `ShoppingTile` — emoji (`IngredientCanon.CANON_INFO`) + nazwa + plakietka „−ilość” (czerwona) albo „✓” (zielona, `MaterialTheme.colorScheme.primary`), stuknięcie kafelka woła to samo `viewModel.toggleChecked(key)` co checkbox w widoku listy. `MainActivity.kt`: `ShoppingScreen` dostał nowy parametr `pantryViewModel` (już współdzielony na poziomie `MainActivity` od FR-15/16), żeby liczyć pokrycie z aktualnego stanu spiżarni. `./gradlew :logic:test :app:compileDebugKotlin` przechodzi (BUILD SUCCESSFUL, wszystkie nowe testy `missingAfterPantry` zielone). **Zweryfikowane wizualnie 2026-08-10**: dotknięcie „🏺 Kafelki (jak w spiżarni)" na liście z 6 pozycjami pokazało siatkę 3-kolumnową z poprawnym emoji per produkt (🍞 chleb żytni, 🧄 czosnek, 🥚 jajka, 🫒 oliwa z oliwek, 🍅 pomidor, 🥬 szpinak) i czerwoną plakietką „−ilość" dla każdej (spiżarnia pusta w tym teście, więc cała ilość brakująca — zgodnie z oczekiwanym fallbackiem). Status podniesiony do ✅.

- **FR-32** (☆ ulubione składniki + „💡 Pomysł na danie”): `DishIdeaGenerator` w `logic/` (testy JUnit, w tym determinizmu z ziarnem `Random`) — jeden-do-jednego port `IDEA_TEMPLATES`/`generateIdea` z `index.html` (te same szablony nazw per kategoria posiłku, ten sam disclaimer w treści metody, wymaga min. 2 ulubionych). Nowy `FavoriteIngredientsViewModel` (`StateFlow<Set<String>>` kanonicznych nazw, współdzielony na poziomie `MainActivity` jak pozostałe ViewModele) zasila dwa miejsca: (1) lista składników na rozwiniętej karcie przepisu (`RecipeCardBody`→`RecipeCard`→`RecipeListWithScrollToTop`) dostała przycisk ☆/★ przed każdym składnikiem (`onToggleFavIngredient`) oraz pogrubienie/kolor `primary` gdy składnik jest ulubiony LUB już śledzony w Spiżarni (`pantryItems.containsKey(canon)`) — port `(have||f)?'have-it'` z web; (2) nowy przycisk „💡 Pomysł na danie z ulubionych składników” nad listą przepisów otwiera `DishIdeaDialog` (nowy dialog w `RecipeListScreen.kt`) z pomysłem dla aktualnie wybranej kategorii (`selectedCategory`), przyciskiem „🔄 Losuj inny pomysł” (re-roll bez zamykania okienka) i tekstem pustego stanu, gdy ulubionych jest mniej niż 2 — jeden-do-jednego z `renderIdea`/pustym stanem z `index.html`. Lokalny stan (bez trwałego zapisu), jak reszta aplikacji przed krokiem 6. `./gradlew :logic:test :app:assembleDebug` przechodzi (BUILD SUCCESSFUL, testy `DishIdeaGeneratorTest` zielone). **W pełni zweryfikowane wizualnie 2026-08-10** (wcześniej tylko częściowo — zobacz poprzednią wersję tej notatki w historii gita o incydencie z „Zaloguj przez Google”; tym razem dotykanie ograniczone wyłącznie do zakładki Przepisy, incydent się nie powtórzył): rozwinięcie karty „Szakszuka…” pokazało listę składników z ☆ przy każdym; dotknięcie gwiazdki przy „2 jajka” i „garść szpinaku” zmieniło oba na wypełnione ★ (pogrubiony, zielony tekst); dotknięcie „💡 Pomysł na danie z ulubionych składników” z tymi dwoma ulubionymi wygenerowało realny, niepusty pomysł „Duszone jajka z szpinak i kaszą” z opisem i stopką „Wygenerowane z Twoich ulubionych składników: jajka, szpinak” — dokładnie zgodnie z kryteriami akceptacji. Zero crashy.

- **FR-48/FR-49/FR-63** (11 motywów kolorystycznych + strukturalne Polaroid/Kafelki): `AppThemes.kt` w `logic/` — jeden-do-jednego port wszystkich 11 bloków `:root[data-theme="..."]` z `index.html` (16 kolorów × 11 motywów, `0xFFRRGGBB` Long, bez zależności od Compose — `logic/` zostaje czystym Kotlinem) + `AppThemesTest.kt` (kolejność/liczba motywów, `byId` fallback, punktowe porównania hexów z kilku motywów w tym ciemnych, oraz test na to że tylko „dark”/„berry” mają `isDark=true` i że tylko „dark” ma swatch inny niż swój `teal` — to samo ryzyko transkrypcji co przy FR-4/FR-29/FR-33's tabelach). `ThemeViewModel` (wzorem `UiScaleViewModel`) trzyma wybrane `themeId`, współdzielony na poziomie `MainActivity` (jak `profileViewModel` itd.) tak żeby CAŁA aplikacja, nie tylko ekran Ustawień, przemalowywała się pod wybrany motyw. `ui/theme/Theme.kt`: `colorSchemeFor(AppThemeDef)` buduje `ColorScheme` Material3 z palety (`--teal`→`primary`, `--honey`→`secondary`, `--plum`→`tertiary`, `--teal-pale`/`--honey-pale`/`--plum-pale`→odpowiednie `*Container`, `--bg`→`background`, `--card`→`surface`, `--line`→`surfaceVariant`/`outline`, `--danger`(-pale)→`error`(Container)) — `onPrimary`/`onSecondary`/`onTertiary`/`onError` zawsze białe, bo `--teal` w `index.html` jest tłem nagłówka/przycisków z `color:#fff` w KAŻDYM motywie, także ciemnych (potwierdzone w źródle: `header{background:linear-gradient(...,var(--teal),...);color:#fff}`). Nowy `LocalDietaThemeId` (CompositionLocal) eksponuje aktywne `themeId` bez przeciągania parametru przez ekrany, które go nie potrzebują — czyta go tylko `RecipeCard`. `SettingsScreen.kt`: nowa karta „🎨 Motyw kolorystyczny” w zakładce Wygląd (FR-71) — `FlowRow` (nie zwykły `Row`, ten sam powód co `PlanPickerDialog`/mikroskładniki wcześniej) 11 `FilterChip` z kolorową kropką (`theme.swatch`) i etykietą, dokładnie te same 11 nazw/kolejność co `THEMES` w `index.html`. FR-49/FR-63's kryterium „nie tylko paleta, ale i kształt” zaimplementowane w `RecipeCard` (`RecipeListScreen.kt`, czyta `LocalDietaThemeId`): Polaroid — ostre rogi (`RoundedCornerShape(3.dp)`), naprzemienna subtelna rotacja ±0.6° wg parzystości indeksu (`itemsIndexed` zamiast `items`, port `:nth-child(even)`), prostująca się (`animateFloatAsState` do 0°) po rozwinięciu karty, grubszy dolny margines (18dp) jako uproszczenie „ramki odbitki”; Kafelki (Metro) — ostre rogi (`RoundedCornerShape(2.dp)`), kolorowy pionowy pasek akcentu 5dp z lewej (`teal` w spoczynku → `honey` po rozwinięciu, port `border-left`), zerowy cień w spoczynku i 4dp po rozwinięciu (port `box-shadow:none` → `0 2px 14px`). Świadomie NIE ported: pary fontów per motyw (`--font-heading`/`--font-body` — wymagałoby pobrania i dołączenia ~12 rodzin Google Fonts jako zasobów Androida, osobny, duży temat sam w sobie) i krzywe/czasy animacji per motyw (`--dur-fast`/`--dur-med`/`--ease` — wymagałoby przeciągnięcia własnego `AnimationSpec` przez wszystkie miejsca z animacją w aplikacji). Status FR-48 pierwotnie ⏳ z dwóch powodów zgodnych z jego własnymi kryteriami akceptacji: (1) wybór motywu, jak reszta stanu w tej wersji, NIE był trwale zapisywany (restart apki wracał do „Zielony”) — **AKTUALIZACJA 2026-08-10: naprawione, patrz notatka „Trwały zapis lokalny”, `themeId` przetrwa teraz restart**; (2) kolor paska statusu systemowego wciąż nie jest synchronizowany z motywem. **AKTUALIZACJA 2026-08-11: naprawione.** `enableEdgeToEdge()` już i tak zostawiał pasek statusu przezroczystym, więc kolorowe tło nagłówka od razu prześwitywało przez niego za darmo — brakującym kawałkiem był tylko kontrast IKON paska statusu, który domyślnie podąża za systemowym trybem dzień/noc, a nie za wybranym motywem appki (rozjazd np. przy motywie „Ciemny” wybranym na telefonie w trybie jasnym). Nowy `SideEffect` w `MainActivity.kt` (kluczowany na `themeId`) liczy jasność (`0.299R+0.587G+0.114B`) z `AppThemeDef.metaColor` — jeden-do-jednego odpowiednik web'owego `<meta name="theme-color">` — i woła `WindowCompat.getInsetsController(window, ...).isAppearanceLightStatusBars = luminance > 0.6`; celowo liczone z `metaColor` (kolor nagłówka), NIE z `isDark` (tryb całej reszty aplikacji) — motyw „Jasny” ma `isDark=false`, ale jego `metaColor` to nasycony niebieski `#2C6FE0`, który i tak potrzebuje jasnych/białych ikon, nie ciemnych; naiwne powiązanie z `isDark` dałoby złe (ciemne, nieczytelne) ikony akurat na tym motywie. Ręcznie zweryfikowane na emulatorze 2026-08-11 zrzutem ekranu: po wybraniu motywu „Jasny” ikony paska statusu (zegar, WiFi, bateria) wyrenderowały się w bieli, w pełni czytelne na niebieskim tle nagłówka. FR-49 i FR-63 dostają ✅ mimo że są "podpunktami" FR-48, bo ich WŁASNE kryteria akceptacji (struktura karty, nie trwałość/pasek statusu) są w pełni spełnione i zweryfikowane. Ręcznie zweryfikowane na emulatorze 2026-08-10 (przez `uiautomator dump` + zrzuty ekranu, w tym ratując się dumpem za każdym razem gdy zrzut nie dawał pewności co do współrzędnych dotknięcia — nawigacja przyciskiem "Wstecz" okazała się bardziej niezawodna niż dotknięcie paska nawigacji dolnej na tym konkretnym emulatorze, przyczyna nieustalona, prawdopodobnie kwestia tego środowiska a nie kodu aplikacji): wybór „Kafelki” w Ustawieniach natychmiast przemalował CAŁĄ aplikację (nagłówek, przyciski, zaznaczone czipy) na niebieski `#2D89EF`; przejście na zakładkę Przepisy pokazało karty z ostrymi rogami, bez cienia i z niebieskim paskiem po lewej stronie każdej karty; rozwinięcie karty zmieniło pasek z niebieskiego na magenta (`#D80073`, `honey` Kafelek) dokładnie jak w CSS; wybór „Różowy” i „Jagodowa noc” (ciemny) też natychmiast przemalowały całą aplikację poprawnymi kolorami (w tym Jagodowa noc poprawnie przełączyła się na ciemne tło z jasnym tekstem); wybór „Polaroid” przemalował nagłówek na terakotowy `#C1503A` i pokazał karty przepisów z widocznie ostrzejszymi rogami niż domyślny motyw (subtelna rotacja ±0.6° jest zgodnie z zamiarem źródła CSS ledwo zauważalna na zrzucie ekranu — to nie błąd, `index.html` też celowo używa tak małej wartości). Zero crashy mimo ok. 20 interakcji i wielu przełączeń motywu/skali w trakcie tej sesji weryfikacyjnej.

- **FR-69 (e-mail/hasło slice) + FR-73 (2026-08-10)**: pierwsza działająca wersja logowania na prawdziwe konto i synchronizacji w chmurze w Androidzie, na wyraźne życzenie użytkownika. Zakres świadomie ograniczony do FR-73's ORYGINALNEJ (v1, sprzed rozszerzenia FR-78) semantyki „ostatni zapis w chmurze wygrywa całym dokumentem" — bez per-pozycyjnego 3-way merge i bez okienka sprzeczności, to osobny, większy port (patrz FR-78, wciąż ⬜ w Androidzie). `CloudSyncCodec.kt` (nowy plik w `logic/`, testy JUnit — round-trip profilu i spiżarni, obsługa `Long` zamiast `Int`/`Double` z Firestore SDK, nierozpoznana wartość enuma → wartość domyślna zamiast wyjątku, brakująca mapa → `null`) zamienia obecnie synchronizowalny wycinek stanu Androida (`displayName`, `profile`, `pantry`, `theme`, `uiScale`, `swipeRatingStyle` — podzbiór pełnej listy pól FR-73, bo tylko tyle istnieje jako realny stan w tej wersji; `favorites`/`myRecipes`/`recipeReviews`/`customTiles`/`communityRecipesEnabled` nieported, więc nic do zsynchronizowania) na/z `Map<String, Any?>`, natywny kształt jakiego Firestore SDK oczekuje wprost (bez pośredniego JSON-a). `AuthViewModel.kt` (nowy, `app/ui/`) — jeden `FirebaseAuth.AuthStateListener` na cały cykl życia: `user==null` → natychmiastowe ponowne logowanie anonimowe (jak `onAuthStateChanged` w `index.html`, appka NIGDY nie zostaje bez żadnego użytkownika — FR-69's kryterium), `user.isAnonymous` → `Anonymous`, inaczej → `SignedIn(uid, email)`; `signUp`/`signIn` przez `createUserWithEmailAndPassword`/`signInWithEmailAndPassword`, `signOut()` (FR-79-podobne, ale bez opcji "wyczyść dane lokalne" — to nieported) po prostu woła `auth.signOut()`, ten sam listener sam z siebie łapie `user==null` i re-loguje anonimowo. **Logowanie Google zaimplementowane 2026-08-10, patrz notatka niżej — czeka na jeden krok w konsoli Firebase, którego nie da się wykonać z tej sesji.** E-mail/hasło działa od razu bez żadnej dodatkowej konfiguracji w konsoli poza samym włączeniem tego dostawcy (co already było zrobione dla wersji web). `CloudAccountCard` w `SettingsScreen.kt` (zakładka Konto) zastąpił dotychczasową deweloperską `FirebaseTestCard`/`FirebaseTestViewModel` (usunięte — realne logowanie samo w sobie dowodzi połączenia z Firebase, karta testowa stała się zbędna) — formularz e-mail/hasło gdy `Anonymous`, status + „Wyloguj się" gdy `SignedIn`. `CloudSyncCoordinator.kt` (nowy, `@Composable` bez UI, wołany raz z `DietaAppRoot`) — `LaunchedEffect` z kluczem na wszystkie synchronizowalne pola, debounce 1500ms jak `scheduleCloudPush()` w `index.html`, `.set(data, SetOptions.merge())` na `users/{uid}` tylko gdy `SignedIn`; `DisposableEffect` z `addSnapshotListener` na tym samym dokumencie aplikuje przychodzące zmiany z powrotem na te same viewmodel-e (`profileViewModel.save`/`setDisplayName`, `pantryViewModel.replaceAll` — nowa metoda, `themeViewModel.setTheme`, `uiScaleViewModel.setScale`, `swipeRatingStyleViewModel.setStyle`), pomijając snapshoty z `metadata.hasPendingWrites()==true` (własny, jeszcze niepotwierdzony przez serwer zapis — standardowy idiom Firestore na uniknięcie odbicia echa, prostszy niż `_lastSyncedSnapshot` z web bo nie robimy tu merge'a per-pozycja). Ręcznie zweryfikowane na emulatorze 2026-08-10 (środowisko deweloperskie na tej maszynie MA dostęp do internetu/Firebase, w odróżnieniu od zastrzeżenia w `FR-73.md`/`FR-78.md`, więc dało się to sprawdzić naprawdę, nie tylko z podstawionym klientem): założenie konta e-mail/hasło zmieniło kartę z formularza na „Zalogowano jako: …" natychmiast; dotknięcie kafelka „cebula” w Spiżarni po zalogowaniu nie wywołało żadnego wyjątku/`PERMISSION_DENIED` w logcacie (ciche powodzenie zapisu do Firestore — SDK nie loguje udanych zapisów, ale też żadnego błędu); `adb shell am force-stop` + ponowne uruchomienie pokazało zarówno stan „Zalogowano jako: testuser1@example.com" (sesja Firebase Auth poprawnie odtworzona z cache'u), jak i kafelek „cebula" nadal śledzony (trwałość lokalna — PantryStore — nienaruszona przez dodanie warstwy synchronizacji). **Czego NIE udało się zweryfikować w tej sesji**: prawdziwej synchronizacji MIĘDZY dwoma urządzeniami (jedno konto zalogowane na dwóch osobnych instancjach appki) — to wymaga drugiego, fizycznego urządzenia, którego tu nie ma; logika po stronie klienta (kodowanie/dekodowanie, debounce, gating po stanie logowania, pomijanie własnego echa) jest zweryfikowana, ale rzeczywisty przepływ dane-z-jednego-urządzenia-na-drugie pozostaje niepotwierdzony, dokładnie jak ta sama luka udokumentowana w `FR-73.md`/`FR-78.md` dla wersji web.

- **FR-69 (Google slice, 2026-08-10)**: `com.google.android.gms:play-services-auth` dodane do `app/build.gradle.kts` (klasyczny `GoogleSignInClient`, nie nowszy Credential Manager — mniej ruchomych części, łatwiejszy do poprawnego spięcia w jednej turze bez możliwości pełnej weryfikacji end-to-end). `AuthViewModel.signInWithGoogleIdToken(idToken)` wymienia token ID Google na `GoogleAuthProvider`/`signInWithCredential` — reszta (`AuthState`, gating `CloudSyncCoordinator`) identyczna jak dla e-mail/hasła. Przycisk „Zaloguj przez Google" w `CloudAccountCard` uruchamia `GoogleSignInClient.signInIntent` przez `rememberLauncherForActivityResult`, `requestIdToken` używa `R.string.default_web_client_id` — string wygenerowany automatycznie przez plugin `google-services` z wpisu `client_type: 3` w `google-services.json` (już tam był, bo logowanie Google jest już włączone dla wersji web), więc nic nie trzeba było wpisywać ręcznie. **Wymagany, jeszcze niewykonany krok w konsoli Firebase, zanim to zadziała naprawdę**: Firebase Console → Ustawienia projektu → aplikacja Android (`com.przemas230.dietaapp`) → „Add fingerprint" → wklej odcisk SHA-1 tego (debugowego) podpisu builda: `53:CA:59:E2:58:94:9A:D8:3F:0A:D6:A1:8C:58:A4:CA:FE:C6:DD:83` (wyliczony z `~/.android/debug.keystore`, alias `androiddebugkey`, hasło domyślne `android` — ten sam klucz podpisuje KAŻDY debugowy build z tej maszyny, więc rejestracja jest jednorazowa). Bez tego kroku Google zwraca `DEVELOPER_ERROR`, co `CloudAccountCard` teraz rozpoznaje i tłumaczy na czytelny komunikat po polsku zamiast gołego kodu błędu. Ręcznie zweryfikowane na emulatorze 2026-08-10 (środowisko MA dostęp do internetu): dotknięcie „Zaloguj przez Google" poprawnie uruchomiło PRAWDZIWY, systemowy ekran logowania Google (nie natychmiastowy błąd — to dowód że samo spięcie intencji/klienta jest poprawne), zero crashy i zero wyjątków w logcacie; anulowanie przepływu (systemowe „Wstecz") poprawnie wróciło do stanu `Anonymous` bez fałszywego komunikatu o błędzie (obsłużone osobno jako `SIGN_IN_CANCELLED`, nie pokazuje się jako „Błąd"). Dokończenie logowania prawdziwym kontem Google NIE zostało sprawdzone (wymaga prawdziwych danych logowania Google, których tu nie ma, plus zarejestrowania SHA-1 powyżej) — to następny krok do zrobienia w Android Studio po wklejeniu odcisku.

- **FR-73 (naprawa + rozszerzenie zakresu, 2026-08-10)**: użytkownik zgłosił, że spiżarnia (i wiele innych rzeczy) w ogóle się nie synchronizuje między web a Androidem, mimo że mechanizm sam w sobie "działał". Przyczyna dla spiżarni: `CloudSyncCodec.encodePantry`/`decodePantry` używały kształtu WEWNĘTRZNEGO dla Androida (`category`/`quantity`/`unit`, nazwa enuma Kotlina zamiast polskiej etykiety) zamiast dokładnego kształtu, jaki web zapisuje do TEGO SAMEGO dokumentu Firestore (`cat`/`qty`/`unitCat`, patrz `index.html`'s `tileTapDelta`/`toggleHaveIngredient`: `{label, cat, type, qty, unitCat}` dla produktu, `{label, cat, type, level}` dla przyprawy) — obie strony pisały poprawnie do tego samego dokumentu, ale żadna nie potrafiła odczytać tego, co zapisała druga, więc `decodePantry` po prostu nic nie aplikował (błędne/nieznalezione pola → `null`/domyślne wartości). Naprawione: `cat`=`PantryCategory.label` (nie `.name`), `qty` (nie `quantity`), `unitCat`=`RecipePantryMatching.pantryUnitCat(unit)` przy zapisie i `PantryTiles.unitCatToUnit(unitCat)` przy odczycie (dokładnie ta sama funkcja, jakiej Android już używa przy tworzeniu nowych wpisów spiżarni — więc odczyt przez `unitCat` niczego nie traci), `level`=`SpiceLevel.label` (nie `.name`). Przy okazji zweryfikowano i naprawiono TĘ SAMĄ klasę błędu z wyprzedzeniem dla pozostałych pól, zamiast czekać na kolejne zgłoszenie: `shopping` (`quantity`→`qty`), `cooked` (data jako epoch millis → ISO 8601 string jak `new Date().toISOString()` w web), `recipeRating` (nazwa enuma `LIKE`/`DISLIKE` → `"like"`/`"dislike"` jak web), `favIngredients` (Android `Set<String>` → mapa `canon:true` jak `state.favIngredients` w web). Poszerzono też ZAKRES o pola, które Android już ma jako realny stan, ale nigdy nie synchronizował: `favIngredients` (FR-32, ten sam dzień), `recipeRating` (FR-55/57, like/dislike), `cooked` (FR-15/17, historia gotowania + oceny gwiazdkowe), `shopping` (FR-25/27, lista zakupów), `planner`/`plannerScale`/`plannerLeftover` (FR-18-24, plan tygodnia — Android trzyma te trzy pola web'a jako jeden `WeekPlan`/`PlannedMeal`, więc `encodePlanner`/`encodePlannerScale`/`encodePlannerLeftover` rozdzielają je na zapis i `decodeWeekPlan` scala z powrotem na odczyt), `eaten`+snacki (FR-36/33/34, dziennik posiłków — zagnieżdżone pod kluczem dzisiejszej daty UTC jak `state.eaten[date]` w web, `date` liczone przez `LocalDate.now(ZoneOffset.UTC)` żeby zgadzało się z web'owym `toISOString().slice(0,10)`; klucz "snacks" wewnątrz tego samego obiektu dnia, tak jak w web), `water` (FR-70, dzisiejsze nawodnienie — `{date, count}`, odczyt ignorowany gdy `date` z chmury nie jest dzisiejszą datą UTC, ten sam "tylko dziś" zakres co reszta tej funkcji w Androidzie). Świadomie NADAL nieporcowane (bo dana funkcja w ogóle nie istnieje jako stan Androida): `myRecipes`/`recipeReviews` (FR-66/67), `customTiles`/`pantryUnitOverride`/`pantryCategoryOverride`, `communityRecipesEnabled`/`household` (FR-68/76/77), `waterHistory`/`weights`/`history` (FR-41/42), `waterNotifEnabled`/`waterReminder` (FR-38/39), zwykły ⭐ przełącznik ulubionych przepisów `favorites` (osobna funkcja od `recipeRating`, nieport w Androidzie w ogóle). `RecipeViewModel` (trzymający `cooked`/`ratings`) był dotąd tworzony jako domyślny parametr `viewModel()` WEWNĄTRZ `RecipeListScreen` — scope'owany do `NavBackStackEntry`, nieosiągalny z `CloudSyncCoordinator` na poziomie `DietaAppRoot` — podniesiony (hoisted) do `MainActivity.kt` na tym samym poziomie co `profileViewModel`/`pantryViewModel` itd., ten sam wzorzec co reszta współdzielonych ViewModeli. Każdy dotknięty ViewModel dostał nową metodę `replaceAll`/`replaceCooked`/`replaceRatings`/`setCount` do aplikowania przychodzącego zapisu z chmury (te same "last-cloud-write-wins" semantyki co reszta FR-73 w Androidzie — bez per-pozycyjnego 3-way merge, to nadal świadomie POZA zakresem, patrz FR-78 wciąż ⬜). `./gradlew :logic:test :app:compileDebugKotlin` przechodzi (BUILD SUCCESSFUL, nowe testy `CloudSyncCodecTest` pokrywające każde nowe pole round-trip). Status ⏳ (nie ✅) bo: rzeczywista synchronizacja między web a Androidem na tym samym koncie NIE została zweryfikowana na dwóch prawdziwych, jednocześnie działających instancjach w tej turze (to samo zastrzeżenie co przy pierwszym porcie FR-73/FR-69) — poprawność jest oceniana przez dokładne porównanie kształtów pól z `index.html` linia po linii, nie przez obserwację żywej wymiany danych.

- **FR-73 (naprawa wyścigu przy szybkim dodawaniu, 2026-08-10)**: użytkownik zgłosił, że "tylko pierwszy artykuł dodaje się z poziomu aplikacji kotlin, każdy następny od razu się zeruje" — dodanie drugiej pozycji do spiżarni (lub dowolnego innego zsynchronizowanego pola) tuż po pierwszej cofało się samo. Przyczyna: 1.5-sekundowe opóźnienie (debounce) przed wysłaniem zmiany do Firestore jest niewidoczne dla `hasPendingWrites()` Firestore SDK — w tym oknie czasowym SDK naprawdę nie ma żadnego oczekującego zapisu, więc potwierdzenie (ack) STARSZEGO zapisu (np. „tylko pozycja 1") mogło dotrzeć PO tym jak stan lokalny zdążył już zmienić się dalej (np. „pozycja 1 + pozycja 2"), z `hasPendingWrites()==false` (poprawnie, ale mylące — nie wie nic o zmianie 2, która czeka jeszcze w debounce, niewysłana). Poprzedni kod widział wtedy zdalne({pozycja1}) != lokalne({pozycja1,pozycja2}) i ślepo nadpisywał lokalne, kasując pozycję 2. Dodatkowo: callback `addSnapshotListener` (żyjący dłużej niż pojedyncza kompozycja) czytał `pantryItems`/itd. jako zwykłe `val`-e z `collectAsState()`, więc widział ich wartość ZAMROŻONĄ w momencie ostatniego uruchomienia `DisposableEffect(uid)` (czyli momentu logowania), a nie aktualną. Naprawione dwuetapowo w `CloudSyncCoordinator.kt`: (1) każda odczytywana wartość owinięta w `rememberUpdatedState`, więc porównania w callbacku zawsze widzą aktualny stan, nie zamrożony; (2) nowy `PushedSnapshot` (prywatna data class) zapamiętuje dokładnie to, co urządzenie OSTATNIO wysłało do Firestore (w momencie wysyłki, nie potwierdzenia) — przychodzący snapshot, który zgadza się z ostatnio wysłaną wartością danego pola, jest rozpoznawany jako spóźnione echo WŁASNEGO zapisu i pomijany, niezależnie od `hasPendingWrites()`/czasu dotarcia. To wciąż oryginalna semantyka FR-73 „ostatni zapis w chmurze wygrywa" (nie pełny 3-way merge z FR-78, który strukturalnie unika tej klasy błędu kosztem dużo większej złożoności) — po prostu poprawnie odróżnia „to mój własny, nieaktualny już zapis" od „to naprawdę nowa zmiana z innego urządzenia". `./gradlew :app:compileDebugKotlin` przechodzi. Status: naprawiono przez analizę kodu i porównanie z zachowaniem web'owego `computeMergedSyncState` (które tej klasy błędu unika strukturalnie dzięki `_lastSyncedSnapshot`), NIE odtworzono błędu na żywo przed naprawą (wymagałoby dwóch szybkich zmian w krótkim odstępie na prawdziwym, zalogowanym urządzeniu) — logika uznana za poprawną przez konstrukcję, czeka na potwierdzenie użytkownika przy następnym realnym użyciu.

- **FR-60/FR-37/FR-40 (zakładka Postęp, 2026-08-10)**: dotychczasowy `PlaceholderScreen` na zakładce Postęp zastąpiony nowym `PostepScreen.kt` — pierwszy realny krok w tej zakładce, nie cała jej zawartość (patrz niżej co świadomie zostało). Trzy karty: (1) FR-60 — „⭐ Złote zasady przy Hashimoto i insulinooporności", warunkowo widoczna tylko gdy `profile.strictLowGI` (jeden-do-jednego port warunku z `index.html`), treść 5 zasad przepisana wprost z `index.html` (statyczny tekst, nic do sparsowania); (2) FR-37 — pełnowymiarowy widok 8 kropelek wody, dzieli TEN SAM `WaterViewModel` co pasek w nagłówku (FR-70), więc dotknięcie kropelki w Postępie i w nagłówku aktualizuje ten sam stan natychmiast w obu miejscach — zamyka kryterium FR-37 „pełny widok i kompaktowy pasek", które wcześniej nie miało drugiej strony do zsynchronizowania; (3) FR-40 — nowy `WeightEntry`/`WeightOperations`/`WeightViewModel` (testy JUnit — walidacja zakresu 30-250 kg jak w web, deduplikacja po dacie: druga waga tego samego dnia PODMIENIA pierwszą zamiast dublować, jak `state.weights = state.weights.filter(w=>w.date!==today)` w `index.html`), pole tekstowe + przycisk „Dodaj", oraz **ręcznie napisany, prosty wykres liniowy na `Canvas`** (bez biblioteki wykresów — ten sam duch co uproszczenia przy FR-64: web używa Chart.js, tu wystarczy linia + punkty + wyróżniony ostatni punkt, bez siatki/etykiet osi) łączący do 30 ostatnich wpisów, plus tekst „Zostało X kg do celu (Y kg)" / „Cel osiągnięty! 🎉" (`WeightOperations.kgToGo`, port `renderWeights`'s `toGo`). Świadomie NIE ported w tej turze (osobne, większe kawałki pracy — każdy wymaga infrastruktury, której Android jeszcze nie ma, nie tylko UI): **FR-38/39** (powiadomienia z akcją „+1 szklanka" i cykliczne przypomnienie o piciu wody) — wymagają kanału powiadomień Androida, uprawnień (`POST_NOTIFICATIONS` na Androidzie 13+) i `WorkManager`/`AlarmManager` do cykliczności, zupełnie inny mechanizm niż web'owy `showNotification`+Service Worker; **FR-41** (wykres historii kalorii + bilans tygodniowy) i **FR-42** (serie/streaks + log aktywności) — obie wymagają, żeby Android w ogóle ZACZĄŁ zapisywać dane PO DACIE (nie tylko „dziś", jak `EatenViewModel`/`WaterViewModel` teraz), czyli nowej warstwy trwałości historii, której jeszcze nie ma nawet szkieletu; log aktywności dodatkowo wymagałby instrumentacji dziesiątek miejsc w kodzie odpowiadających web'owym rozsianym wywołaniom `addLog()`. Ręcznie zweryfikowane na emulatorze 2026-08-10: świeża instalacja pokazała wszystkie trzy karty (zasady widoczne, bo `strictLowGI` domyślnie `true`); dotknięcie 3. kropelki wody ustawiło „3/8" jednocześnie w Postępie I w pasku nagłówka (potwierdza współdzielony `WaterViewModel`); wpisanie „68.5" i „Dodaj" pokazało wykres z jednym wyróżnionym punktem i tekst „Zostało 6.5 kg do celu (62 kg)" (68.5-62=6.5, poprawne wyliczenie). Zero crashy.

- **FR-66/FR-67 (własne przepisy + ocena gwiazdkowa, 2026-08-10)**: oba na wyraźną prośbę użytkownika ("możesz zrobić teraz ocenianie przepisów i dodawanie swoich przez użytkowników"). **FR-66**: `Recipe` (w `logic/`) dostał nowe pole `source: String = "builtin"` (domyślne, więc `RecipeRepository`'s parsowanie JSON-a nie wymagało zmian — każdy z 229 wbudowanych przepisów automatycznie ma `"builtin"`) — `"custom"` oznacza przepis dodany przez użytkownika, jak `state.myRecipes`/`source:"custom"` w `index.html`. Nowy `CustomRecipeOperations` w `logic/` (testy JUnit) — `validate`/`build` portujące walidację formularza `index.html` (nazwa i ≥1 składnik wymagane, kategoria/czas/sposób przygotowania mają rozsądne wartości domyślne gdy puste, kalorie muszą być dodatnią liczbą). **Świadomie NIE ported**: automatyczne wyliczanie kalorii/makroskładników z tekstu składników (`estimateRecipeMacrosFromText`/`INGREDIENT_MACRO_DB`, ~90 pozycji) — ręczne wpisanie kcal (wymagane) i białka/węgli/tłuszczu (opcjonalne) jest w `index.html` ZAWSZE poprawną, w pełni wspieraną ścieżką (auto-wyliczanie tylko wypełnia pola wstępnie, użytkownik może je nadpisać), więc pokrywa to rzeczywiste kryteria akceptacji FR-66 — port samej tabeli ~90 składników + logiki dopasowania to osobny kawałek praktycznie tej samej wielkości co `SnackNutritionDb` (FR-33/34), odłożony na później. `RecipeViewModel` doszedł `myRecipes: StateFlow<List<Recipe>>` + `addCustomRecipe`/`removeCustomRecipe`, `builtInRecipes + myRecipes` łączone przy każdym przeliczeniu widocznej listy (port `allRecipes()`). `AddCustomRecipeDialog` w `RecipeListScreen.kt` — pola nazwa/kategoria (pigułki 5 kategorii Planera)/czas/składniki (jeden na linię)/sposób przygotowania/kcal/B/W/T, błąd walidacji pokazany bezpośrednio pod formularzem (nie tylko toast, zgodnie ze zrewidowanym kryterium FR-66 o widocznym komunikacie). Karta przepisu: plakietka „✍️ Twój przepis" obok nazwy gdy `source=="custom"`, przycisk „🗑️ Usuń ten przepis" (z `AlertDialog` potwierdzenia, tekst wprost mówiący że historia gotowania/lista zakupów z tego przepisu nie zostaną naruszone) widoczny tylko dla własnych przepisów — usunięcie NIE czyści `cooked`/`shopping` (te wpisy trzymają się `recipeId`, nie referencji do obiektu, więc naturalnie przeżywają usunięcie, zgodnie z kryterium). **FR-67**: nowy `RecipeReview`(`data/`)/`RecipeReviewOperations`(`logic/`, testy JUnit) — port `state.recipeReviews[id]={stars,comment,at}` i sortowania 🏆 malejąco (nieocenione na końcu, `sortedByDescending{stars?:0}`, stabilne). `RecipeViewModel.reviews`+`setReview`/`clearReview` (walidacja 1-5 gwiazdek jak `index.html`'s "Wybierz od 1 do 5 gwiazdek"; pusty/białoznakowy komentarz normalizowany do `null`, nie pustego stringa). `RecipeReviewDialog` w `RecipeListScreen.kt` — reużywa istniejący `StarRatingRow` (ten sam widżet co FR-17's ocena w historii gotowania), pole komentarza z licznikiem „N/300" i twardym limitem długości, przycisk „Usuń ocenę" widoczny tylko gdy już oceniono, ponowne otwarcie dla ocenionego przepisu wstępnie wypełnia gwiazdki i komentarz (kryterium akceptacji). Przycisk „⭐ Oceń i skomentuj" (z liczbą gwiazdek w etykiecie gdy ocenione) i wyświetlony komentarz umieszczone na samym dole rozwiniętej karty, POD składnikami i sposobem przygotowania — dokładnie tak jak w `index.html`. Nowa pigułka „🏆 Ocena" w pasku filtrów, niezależna od 🎯/❤️ (te same zasady kolejności co pozostałe przełączniki sortowania). **Świadomie NIE ported**: wielo-użytkownikowy wątek komentarzy pod przepisem (FR-77, `recipes/{id}/ratings` z paginacją) i publikowanie własnej oceny do współdzielonej kolekcji (`pushRecipeRating`/`publicProfiles/{uid}/reviewedRecipes`) — to osobna, większa funkcja wymagająca prawdziwych zapytań/paginacji Firestore do kolekcji społecznościowej, którą Android jeszcze w ogóle nie dotyka (FR-76/77 nadal ⬜); ocena i komentarz w tej wersji Androida są w pełni lokalne, jak reszta jeszcze niesynchronizowanej z chmurą funkcjonalności (patrz notatka FR-73 wyżej — `myRecipes`/`recipeReviews` świadomie nie są jeszcze w `CloudSyncCodec`). `./gradlew :logic:test :app:assembleDebug` przechodzi (BUILD SUCCESSFUL, nowe testy `CustomRecipeOperationsTest`/`RecipeReviewOperationsTest` zielone). Ręcznie zweryfikowane na emulatorze 2026-08-10: świeża instalacja, budowa i uruchomienie bez crasha; UI obu funkcji renderuje się poprawnie przez inspekcję kodu i strukturę identyczną z już zweryfikowanymi wzorcami w tym samym pliku (np. `AddCustomRecipeDialog` używa dokładnie tego samego `FlowRow`+`FilterChip` wzorca co już zweryfikowany `PlanPickerDialog`) — pełny przepływ dodania własnego przepisu i zapisania oceny przez dotknięcia na emulatorze NIE został osobno przeklikany w tej turze (bardzo duży zakres zmian w jednej sesji), więc status ⏳ zamiast ✅ dla obu, czeka na pierwsze realne użycie.

- **Trwały zapis lokalny (2026-08-10)**: przed tą zmianą DOSŁOWNIE cały stan Androida (spiżarnia, lista zakupów, planer, profil, motyw, skala UI, historia gotowania, oceny, ulubione składniki, własne przepisy, recenzje, waga, dzisiejsze nawodnienie/posiłki) żył WYŁĄCZNIE w pamięci `ViewModel`i — `am force-stop` albo zwykłe zamknięcie appki przez system kasowało wszystko, co jest złym pierwszym wrażeniem i realnym ryzykiem utraty danych, niezależnie od tego czy użytkownik jest zalogowany na chmurę czy nie (anonimowi użytkownicy nie mają NIC innego). Naprawione nowym `LocalStateStore` (`data/`, potrzebuje `Context` więc żyje w `app/`, nie w `logic/`) — zwykły plik JSON w `context.filesDir` (`local_state.json`), androidowy odpowiednik web'owego `localStorage.setItem(STORE_KEY, JSON.stringify(state))`/`saveState()`. Kluczowa decyzja projektowa: **żaden nowy kodek** — `LocalStateStore` konwertuje surowy JSON ↔ `Map<String, Any?>` (własne, bezpieczne rekurencyjne `mapToJson`/`jsonObjectToMap` na `org.json`, bez polegania na wersyjnie-niepewnych metodach typu `JSONObject.toMap()`), a WSZYSTKIE encode/decode per-pole to te same funkcje z `CloudSyncCodec`, których `CloudSyncCoordinator` już używa do chmury — jeden kodek, dwa cele (dysk lokalny i Firestore), więc pole raz poprawnie zakodowane/zdekodowane (patrz notatka FR-73 o naprawionym błędzie spiżarni) automatycznie działa poprawnie w OBU miejscach. `CloudSyncCodec` dostał trzy nowe pary `encode`/`decode` dla pól, które dotąd nie miały żadnego kodeka bo nie są (jeszcze) synchronizowane z chmurą, ale SĄ lokalnym stanem: `encodeMyRecipes`/`decodeMyRecipes` (FR-66, filtruje `source=="custom"` — 229 wbudowanych przepisów nigdy nie jest zapisywanych, ładują się z `recipes.json` przy każdym starcie), `encodeReviews`/`decodeReviews` (FR-67), `encodeWeights`/`decodeWeights` (FR-40) — wszystkie z testami JUnit (round-trip). Nowy `LocalPersistenceCoordinator.kt` (`ui/`, wzorem `CloudSyncCoordinator` ale bez sieci) — jeden `LaunchedEffect(Unit)` przy starcie ładuje plik i aplikuje na WSZYSTKIE ViewModel-e naraz (w tym te bez odpowiednika w chmurze: `recipeViewModel.replaceMyRecipes`/`replaceReviews`, `weightViewModel.replaceAll` — nowe metody), flaga `initialLoadDone` chroni drugi `LaunchedEffect` (właściwy zapis, debounce 500ms — krótszy niż chmurowe 1.5s, bo zapis na dysk nie kosztuje sieci) przed nadpisaniem świeżo wczytanego pliku pustymi wartościami domyślnymi ViewModeli sprzed zakończenia wczytywania. Działa dla KAŻDEGO użytkownika (anonimowego i zalogowanego) — w odróżnieniu od `CloudSyncCoordinator`, który nadal działa tylko po prawdziwym zalogowaniu; kolejność przy starcie zalogowanego urządzenia: najpierw stan lokalny z pliku, potem (gdy `AuthViewModel` potwierdzi sesję z poprzedniej instalacji) `CloudSyncCoordinator`'s listener nakłada na to dane z chmury — dokładnie ten sam dwuwarstwowy model co web (`localStorage` przy starcie + `attachUserDocListener` po rozpoznaniu sesji Firebase). `RecipeViewModel`/`WeightViewModel` dostały brakujące metody `replaceMyRecipes`/`replaceReviews`/`replaceAll` potrzebne do zaaplikowania wczytanego stanu. Podniesiono status kilku FR-ów, których JEDYNYM pozostałym blokerem był brak trwałości: **FR-14** (skala UI), **FR-65** (nazwa użytkownika, drugi bloker — brak zakładek FR-71 — też już nieaktualny), **FR-70** (nawodnienie, drugi bloker — placeholder Postępu — też już nieaktualny) podniesione do ✅; **FR-48** (motyw) i **FR-26** (lista zakupów) mają teraz JEDEN pozostały bloker zamiast dwóch (odpowiednio: kolor paska statusu, udostępnianie przez Intent) — patrz zaktualizowane notatki przy każdym z nich. `./gradlew :logic:test :app:compileDebugKotlin` przechodzi. Ręcznie zweryfikowane na emulatorze 2026-08-10 — najbardziej przekonujący możliwy test, dokładnie ten sam scenariusz co pierwotne zgłoszenie użytkownika o resetującej się spiżarni: `pm clear` (czysty start) → dotknięcie kafelka „cebula” w Spiżarni → `am force-stop` → ponowne uruchomienie → kafelek „cebula” nadal pokazywał plakietkę „1” z zieloną obwódką, czyli PRZETRWAŁ pełne zamknięcie procesu, nie tylko zmianę ekranu. Zero crashy.

- **FR-79 (dwuetapowe okienko wylogowania + czyszczenie danych, 2026-08-10)**: `CloudAccountCard` w `SettingsScreen.kt` miała już samo `viewModel.signOut()` bez żadnego potwierdzenia — dodane teraz DWA osobne pytania, dokładnie jak opisuje FR: (1) „Wylogować się z tego urządzenia?" (Wyloguj/Anuluj); (2) TYLKO po potwierdzeniu (1) — „Wyczyścić też dane lokalne?" (Wyczyść/Nie czyść), gdzie „Nie czyść" jest udokumentowanym domyślnym zachowaniem — zarówno kliknięcie tego przycisku, JAK I zamknięcie okienka w inny sposób (przycisk „Wstecz”, dotknięcie tła) prowadzi do tej samej ścieżki (`proceedKeepingData`), nie anuluje całego wylogowania. `signOut()` woła się ZAWSZE po rozstrzygnięciu drugiego pytania, niezależnie od wyboru — zgodnie z Opisem FR-79 ("W obu przypadkach aplikacja... wywołuje signOut()"). Nowy parametr `SettingsScreen(onClearLocalData: () -> Unit)` — `CloudAccountCard` sam nie ma dostępu do wszystkich ViewModeli, więc `MainActivity.kt` (jedyne miejsce, gdzie WSZYSTKIE są już hoisted) dostarcza jedną dużą lambdę resetującą po kolei: `profileViewModel.resetToDefault()`+`setDisplayName("")`, `pantryViewModel`/`shoppingViewModel`/`plannerViewModel.replaceAll(emptyMap())`, `recipeViewModel.replaceCooked`/`replaceRatings`/`replaceReviews(emptyMap())`+`replaceMyRecipes(emptyList())`, `favoriteIngredientsViewModel.replaceAll(emptySet())`, `eatenViewModel.replaceAll(emptyMap(), emptyList())`, `waterViewModel.setCount(0)`, `weightViewModel.replaceAll(emptyList())`, `themeViewModel.setTheme(AppThemes.DEFAULT_ID)`, nowa `uiScaleViewModel.resetToAuto()` (przywraca `null` = auto-detekcja, jedyny sposób cofnięcia ręcznie ustawionej skali), `swipeRatingStyleViewModel.setStyle(BALLOON)`. Nie trzeba osobno kasować pliku `LocalStateStore` — `LocalPersistenceCoordinator` obserwuje te same StateFlow'y i sam dopisze świeże, puste wartości przy najbliższym debounce, dokładnie jak każda inna zmiana stanu. `./gradlew :logic:test :app:compileDebugKotlin` przechodzi. Status ✅ oceniany przez inspekcję kodu i zgodność z już zweryfikowanym wzorcem `AlertDialog` (ten sam co potwierdzenie usunięcia własnego przepisu, FR-66) — sam dwuetapowy przepływ dotknięć nie został osobno przeklikany na emulatorze w tej turze.

- **FR-41/FR-42 (historia kalorii + serie, 2026-08-10)**: dotąd oba wymagały czegoś, czego Android w ogóle nie miał — historii PO DACIE zamiast tylko „dziś" — więc były świadomie odłożone razem z notatką FR-60/FR-37/FR-40. Trwały zapis lokalny (patrz notatka niżej) usunął tę przeszkodę: `EatenViewModel` dostał `kcalHistory: StateFlow<Map<String, Int>>` — przy KAŻDEJ zmianie `entries`/`snacks` (`toggle`/`addSnack`/`removeSnack`/`replaceAll`) prywatna `recordTodayInHistory()` przelicza `EatenOperations.dailyEatenKcal(...)+snacksKcal(...)` i dopisuje wynik pod klucz dzisiejszej daty UTC — dokładnie ten mechanizm, przez który `index.html`'s `state.eaten[date]` naturalnie gromadzi historię, tylko jawny zamiast dorozumiany (Android nie ma ekranu do przełączania się między datami, więc nie ma innego miejsca, w którym ta aktualizacja mogłaby się zdarzyć). `WaterViewModel` dostał analogiczny `history: StateFlow<Map<String, Int>>`, aktualizowany w `tapDroplet`/`setCount`. Nowy `HistoryOperations` w `logic/` (testy JUnit z wstrzykiwaną datą `today` dla determinizmu) — jeden-do-jednego port `calcKcalStreak`/`calcWaterStreak` (włącznie z zasadą „dzisiaj nie przerywa serii, jeśli jeszcze nie spełnia celu" — pętla wstecz od dziś, `i===0` pomija zamiast przerywać) oraz `weeklyBalance`/`lastNDays` dla wykresu i bilansu. `PostepScreen.kt`: nowa karta „🔥 Serie" (dwa kafelki, port `streaksBox`) i „📈 Historia kalorii" — prosty słupkowy `Canvas` (14 dni, ten sam „bez biblioteki wykresów" duch co `WeightChart`) z przerywaną linią celu dziennego (`PathEffect.dashPathEffect`) i tekstem bilansu 7 dni, port `kcalHistoryChartCanvas`/`kcalWeeklyBalance`. `CloudSyncCodec` dostał generyczny `encodeDateIntMap`/`decodeDateIntMap` (testy JUnit) — jeden kodek dla obu nowych map, na razie tylko do trwałego zapisu lokalnego (nie synchronizowane z chmurą, jak `myRecipes`/`recipeReviews`/`weights` wcześniej). `MainActivity`'s „wyczyść dane lokalne" (FR-79) doszły `eatenViewModel.replaceHistory(emptyMap())`/`waterViewModel.replaceHistory(emptyMap())`. **Ograniczenie**: historia startuje od zera od dnia wdrożenia tej funkcji — nie ma jak odtworzyć danych sprzed niej, ten sam rodzaj uczciwego ograniczenia co reszta tej sesji. Ręcznie zweryfikowane na emulatorze 2026-08-10 — najbardziej przekonujący możliwy test, na żywo: świeża instalacja pokazała „🔥 Serie" z 0/0, dotknięcie 8. kropelki wody (do 8/8) NATYCHMIAST zaktualizowało kafelek „dni z pełnym nawodnieniem" z 0 na 1 (licząc dzisiejszy, jeszcze niezakończony dzień jako już kwalifikujący się, zgodnie z regułą źródła), karta „Historia kalorii" wyrenderowała słupkowy wykres 14 dni z widoczną przerywaną linią celu. Zero crashy.

- **FR-42 (surowy log aktywności, 2026-08-10)**: dokończenie FR-42 — poprzednia notatka wyżej zostawiła to świadomie odłożone, bo wymagało instrumentowania każdego miejsca w kodzie Androida odpowiadającego web'owym rozsianym wywołaniom `addLog()`. Policzone dokładnie w `index.html`: 14 wywołań `addLog()` w 6 typach akcji (`pantry_add`×5, `pantry_delete`×3, `cook_subtract`×2, `shopping_add`×2, `shopping_remove`×1, `pantry_recat`×1 — niektóre funkcje jak haveBtn-toggle logują różne akcje zależnie od kierunku zmiany, nie stałą). Nowe pliki: `data/ActivityLogEntry.kt` (`tsEpochMillis`/`action`/`detail`), `logic/ActivityLogOperations.kt` (testy JUnit) — port `addLog()` (`MAX_ENTRIES=200`, prepend+cap jak `state.history.unshift`+`length>200`) i `filterByDateRange` (port renderHistory's Od/Do filtr, oba brzegi inclusive), `ui/ActivityLogViewModel.kt` (`entries`/`log`/`clear`/`replaceAll`) — hoisted w `MainActivity.kt` obok `weightViewModel`, współdzielony między `PantryScreen`/`RecipeListScreen`/`PostepScreen`/`LocalPersistenceCoordinator`. Wpięte 1:1 wg web'owych call site'ów: `PantryScreen.kt` (dotknięcie kafelka, dodanie własnego produktu, zmiana kategorii, usunięcie śledzenia), `RecipeListScreen.kt` (ugotowanie dziś, cofnięcie wpisu ugotowania, „Mam to" w liście składników — rozróżnia `pantry_add`/`pantry_delete` po tym czy pozycja była w spiżarni PRZED dotknięciem, dokładnie jak web'owy `haveBtn` handler, dodanie pojedynczego składnika do zakupów, dodanie/usunięcie całego przepisu z zakupów). `ShoppingScreen.kt` NIE dostał żadnych nowych wywołań — jego dwie bulk-add funkcje (`addDayPlanWithMessage`/`addWeekPlan`, dodawanie całego dnia/tygodnia z Planera) nie mają odpowiednika w web'owej liście 14 wywołań `addLog()`, więc nie powinny logować. `PostepScreen.kt` dostał nową kartę „📜 Historia aktywności" (port sekcji `#historyList` z `index.html`) — pola tekstowe Od/Do (format RRRR-MM-DD, zwykły `OutlinedTextField` zamiast naiwnego date-pickera, ten sam „bez dodatkowych zależności" duch co reszta appki), domyślny limit 20 najnowszych wpisów z przyciskiem „Pokaż całą historię (N)"/„Pokaż tylko 20 ostatnich" (ukryty podczas filtrowania, jak w web), przycisk „Wyczyść" z `AlertDialog` potwierdzenia (port `confirm("Wyczyścić całą historię aktywności?")`), pusty stan gdy brak wpisów vs brak wpisów w wybranym zakresie dat (dwa osobne komunikaty, jak web). `CloudSyncCodec` dostał `encodeActivityLog`/`decodeActivityLog` (testy JUnit, round-trip) — w chwili wdrożenia tylko do trwałego zapisu lokalnego (`LocalPersistenceCoordinator` wczytuje/zapisuje log pod kluczem `activityLog`), synchronizacja z chmurą dołączona kilka godzin później tego samego dnia, patrz notatka „Rozszerzenie synchronizacji chmury" niżej (tam log trafia pod web'ową nazwę pola `history`, nie `activityLog`). `MainActivity`'s „wyczyść dane lokalne" (FR-79) doszło `activityLogViewModel.clear()`. `versionCode` 30→31, `versionName` 0.1.29→0.1.30. `./gradlew :logic:test :app:assembleDebug` przechodzi (BUILD SUCCESSFUL, zero failures). Ręcznie zweryfikowane na emulatorze 2026-08-10 — najbardziej przekonujący możliwy test, na żywo: świeża instalacja (dane z poprzedniej sesji), dotknięcie „🛒 Dodaj do listy zakupów" na przepisie „Szakszuka ze szpinakiem i pomidorami" w zakładce Przepisy, przejście do Postępu → nowa karta „Historia aktywności" natychmiast pokazała wpis „10.08.2026, 14:50 — Dodano do listy zakupów: Szakszuka ze szpinakiem i pomidorami"; `am force-stop` + ponowne uruchomienie → ten sam wpis nadal widoczny (trwałość lokalna działa dla nowego pola tak samo jak dla reszty stanu). Zero crashy.

- **Rozszerzenie synchronizacji chmury + naprawa niszczenia historii (2026-08-10)**: użytkownik zgłosił po pierwszych realnych testach obu wersji naraz, że parametry diety (cel — redukcja/masa) i wypita woda nie synchronizują się między web a Androidem ("wypita woda tu swoje a tu swoje"), że historia też powinna być wspólna, a nie jest, oraz że okienko potwierdzenia synchronizacji w web wyskakuje meczaco często i powinno pojawiać się tylko po powrocie z offline. Cztery zmiany, jedna web + trzy Android:
  1. **Web (FR-78, index.html)**: opisane osobno w Historii rewizji FR-78/v4 — okienko „🔄 Synchronizacja z chmury" pokazuje się już tylko dla prawdziwych sprzeczności (`conflicts.length`), nie dla każdej bezkolizyjnej zmiany zdalnej (dawniej dodatkowo dla `pantryRemoteChanges.length`, co przy używaniu Androida i weba naraz oznaczało niemal ciągłe popupy). `versions/v70/`, Service Worker `v44→v45`.
  2. **Naprawiony realny błąd niszczenia historii**: `state.eaten`/`state.waterHistory` w web to mapy PO DACIE, narastające miesiącami (`state.eaten[date]`, `state.waterHistory[date]`) — Android zna tylko „dziś" lokalnie i pchał to jako zwykłe pole najwyższego poziomu przez `SetOptions.merge()`, co w Firestore w pełni ZASTĘPUJE całą wartość tego pola, nie scala go głębiej. Każdy dotychczasowy push z Androida od momentu wdrożenia synchronizacji `eaten` (FR-73, wcześniej w tej samej turze) realnie KASOWAŁ wszystkie inne dni w historii posiłków web'a, zostawiając tylko dzisiejszy wpis — potwierdzony, poważny błąd utraty danych, nie tylko teoretyczne ryzyko. Naprawione przez `SetOptions.mergeFields("eaten.$today", "waterHistory.$today", ...)` zamiast zwykłego `SetOptions.merge()` — Firestore'owa kropkowana ścieżka scala WYŁĄCZNIE ten jeden zagnieżdżony klucz, nie ruszając żadnej innej daty w dokumencie. Pozostałe pola (`profile`, `pantry`, `shopping`, `weights`, `history`...) nadal idą jako zwykłe pełne zastąpienie pola najwyższego poziomu — to bezpieczne, bo nie są mapami narastającymi po dacie (dokładnie tak samo traktuje je sam web — żadne z nich nie jest w jego własnej liście `MAP_MERGE_KEYS`).
  3. **Dołączono do synchronizacji cloud**: `weights` (historia wagi, FR-40) i `history` (log aktywności, FR-42 — celowo nazwa pola `history`, NIE `activityLog`, żeby zgadzać się z web'ową nazwą klucza w tym samym dokumencie Firestore) jako zwykłe pełne pola (ten sam poziom ryzyka co web już akceptuje dla tych pól), oraz `waterHistory` z osobnym traktowaniem: PUSH tylko dzisiejszego wpisu przez zagnieżdżoną ścieżkę (jak `eaten` wyżej), PULL scala (sumuje) całą zdalną mapę z lokalną zamiast ją zastępować — lokalny wpis „dziś" zawsze wygrywa przy kolizji klucza (bo to własny, żywy licznik urządzenia, synchronizowany osobno przez pole `water`), a wszystkie inne daty przychodzące z chmury (których Android nigdy lokalnie nie miał) są zachowywane. `myRecipes`/`recipeReviews` świadomie NIE dołączone w tej turze (użytkownik nie prosił o nie explicité, mniejszy priorytet, żeby nie rozdymać zmiany bardziej niż trzeba).
  4. **Zabezpieczenie przed wyścigiem push-przed-pierwszym-pull**: `CloudSyncCoordinator` mógł wcześniej wypchnąć jeszcze-nie-zsynchronizowany, lokalny (domyślny/nieaktualny) stan Androida do Firestore ZANIM w ogóle dotarł pierwszy snapshot z konta (np. świeże logowanie na już używane konto, wolne połączenie) — realne ryzyko nadpisania prawdziwych danych konta (np. profilu z inną wartością `goal`) świeżo podłączonego urządzenia, zanim zdążyło cokolwiek realnie pobrać. Naprawione nowym `hasReceivedFirstSnapshot` (wzorem `initialLoadDone` z `LocalPersistenceCoordinator`) — blokuje push dopóki nie przetworzono co najmniej jednego snapshotu (w tym przypadku „dokument jeszcze nie istnieje", co też się liczy — nie ma czego stracić).
  `CloudSyncCodec.todayUtcDateString()` upublicznione (było `private`) żeby `CloudSyncCoordinator` mógł zbudować dokładną ścieżkę `"eaten.$today"` bez duplikowania obliczenia. `./gradlew :logic:test :app:assembleDebug` przechodzi (BUILD SUCCESSFUL). Ręcznie zweryfikowane na emulatorze 2026-08-10 tylko na poziomie „aplikacja się kompiluje, instaluje i uruchamia bez crasha, kontynuuje normalną pracę" — **NIE zweryfikowane na żywo między dwoma prawdziwie zsynchronizowanymi urządzeniami** w tej turze: emulator w chwili testu był w stanie anonimowym (niezalogowany), więc cała nowa ścieżka kodu (push-gate, `mergeFields`, scalanie `waterHistory`) nie została realnie wywołana wobec Firestore. Poprawność oceniana przez dokładną analizę kodu web'owego (`state.eaten[date]`/`SYNCED_STATE_KEYS`/`MAP_MERGE_KEYS`) i budowę/testy jednostkowe, tak jak poprzednie tury prac nad FR-73 — czeka na potwierdzenie użytkownika przy następnym realnym użyciu na dwóch zalogowanych urządzeniach jednocześnie.

- **FR-68/76/77 (2026-08-11)**: port „warstwy społecznościowej" — przełącznik przepisów społeczności (FR-68), publikowanie własnych przepisów + przeglądanie użytkowników/profili (FR-76) i wielo-użytkownikowe komentarze z paginacją (FR-77) — na życzenie użytkownika, po odblokowaniu logowania Google (FR-69, odcisk SHA-1 dodany przez użytkownika w konsoli Firebase). Nowy `logic/.../CommunityRecipeOperations.kt` (testy JUnit) — port `sanitizeCommunityRecipeDoc`/`sanitizeRatingDoc`/dedupe z `index.html:3010-3039,5652-5663`, **bez odpowiednika `escapeHtml`**: Compose'owy `Text()` nigdy nie interpretuje swojego argumentu jako znaczników (w odróżnieniu od web'owego `innerHTML`), więc nie ma tu wektora XSS do zamykania — świadome uproszczenie, nie przeoczenie. Nowy `app/.../ui/CommunityCoordinator.kt` (Composable, wzorem `CloudSyncCoordinator`, aktywny wyłącznie dla `AuthState.SignedIn`, każdy zapis best-effort/nigdy niepsujący UI): `touchPublicProfile()` przy każdym wejściu w stan zalogowany; publikacja/usuwanie własnych przepisów w `recipes/{id}` (status `"pending"`) reaktywnie, przez diff `myRecipes` względem baseline ustalonego PRZY PIERWSZYM URUCHOMIENIU koordynatora (nie przy każdym starcie aplikacji) — celowo, żeby restart appki nie resetował już zatwierdzonego przepisu z powrotem na `"pending"`; subskrypcja `recipes.where(status==approved)` aktywna tylko gdy zalogowany NA PRAWDZIWE konto i przełącznik włączony; analogiczny diff-z-baseline dla `recipes/{id}/ratings/{uid}` + `publicProfiles/{uid}/reviewedRecipes/{id}` przy zapisie/usunięciu własnej recenzji (FR-67). `RecipeViewModel` dostał `communityRecipesEnabled` (persystowany lokalnie + synchronizowany jako zwykłe pole skalarne, last-write-wins — nie wymaga czekania na FR-78) i `communityRecipes` (lista z Firestore, dedupe względem `myRecipes` w `recompute()`). Nowy `app/.../ui/RecipeCommentsViewModel.kt` — paginacja `recipes/{id}/ratings` (3 → +10, `Query.startAfter`), stan per-recipeId, wołany z nowej sekcji „💬 Komentarze innych użytkowników” w `RecipeCardBody` (pod przyciskiem „⭐ Oceń i skomentuj”, `RecipeListScreen.kt`). Nowe `UserListScreen`/`UserProfileScreen` (+ `UserListViewModel`/`UserProfileViewModel`) jako PEŁNOEKRANOWE widoki zamiast web'owych modali (spójne z resztą Androida), osiągalne z nowej karty „🌍 Przepisy społeczności” w Ustawieniach → Konto (przełącznik + przycisk „👥 Przeglądaj użytkowników”); nowe trasy `Screen.UserList`/`Screen.UserProfile("{uid}")` w `ui/navigation/Screen.kt`, poza `BOTTOM_NAV_SCREENS`. Plakietka karty przepisu rozróżnia teraz „✍️ Twój przepis” vs „🌍 {autor}”; przełącznik „🧑‍🍳 Moje przepisy” rozszerzony na `source=="custom" || source=="community"` (kryterium FR-76). `./gradlew :app:assembleDebug :logic:test :app:testDebugUnitTest` przechodzi (8 nowych testów `CommunityRecipeOperationsTest`, 0 błędów), `versionCode` 42→43, `versionName` 0.1.41→0.1.42, `dist/app-debug.apk`+`dist/version.json` odświeżone. **Nie zweryfikowane na żywo** (świadomie, wymaga dwóch kroków spoza tej sesji): (1) wklejenia reguł Firestore z `docs/FIREBASE_MIGRATION_PLAN.md:162-211` w konsoli Firebase (bez nich każda z tych funkcji bezpiecznie pokazuje pusty/niedostępny stan zamiast crasha lub błędu — to oczekiwane, nie usterka), (2) drugiego prawdziwego konta do faktycznego przetestowania widoczności zatwierdzonego przepisu, listy użytkowników i komentarzy między dwoma urządzeniami. FR-78 (pełne 3-way merge dla `users/{uid}`) świadomie ODŁOŻONE na osobną turę — architektonicznie niezależne od powyższego (inne kolekcje Firestore), duża i ryzykowna przebudowa istniejącego, działającego `CloudSyncCoordinator`, zgodnie z zasadą CLAUDE.md o niepiętrzeniu wielu niezweryfikowanych kroków naraz.

- **Restylizacja pod wspólny design system, Faza 1/6 — paleta (2026-08-10)**: użytkownik przesłał zrzuty ekranu referencyjnej aplikacji do liczenia kalorii i poprosił o ujednolicenie wyglądu web+Android pod tę samą identyczność (zielony nagłówek, musztardowy akcent, dwukolorowy pierścień kalorii/wody, kubeczkowe nawodnienie, "Twoja seria", "Droga do celu"). Ustalone z użytkownikiem PRZED rozpoczęciem pracy (patrz plan w tej sesji): backend zostaje Firestore (bez migracji na Realtime Database, żadna z dzisiejszych napraw synchronizacji się nie cofa), nawigacja zostaje bez zmian strukturalnych (te same 5 zakładek), zdjęcia przepisów i prawdziwa integracja AI do szacowania kalorii świadomie POZA zakresem (osobne, przyszłe tematy). Praca podzielona na 6 faz, każda weryfikowana osobno w web i Kotlinie przed przejściem dalej — to notatka dla **Fazy 1: paleta**. Kluczowe odkrycie: appka już ma system 11 wymienialnych motywów (FR-48/49/63) i domyślny motyw „teal” był już koncepcyjnie blisko referencji (zielony nagłówek + złoty akcent), więc restylizacja to DOPRECYZOWANIE istniejących hex-ów motywu „teal”/„dark”, nie nowa infrastruktura — patrz FR-48's Historia rewizji v3 dla dokładnych wartości przed/po (nagłówek `#1B5E3F -> #2E7D5B`, akcent `#F5A623`, karty 16px -> 26px promienia, „Ciemny” dostał TĘ SAMĄ zieloną/musztardową tożsamość co domyślny zamiast własnej odrębnej, tło/karty prawie czarne `#0D0D0D`/`#1A1A1A`). Zmienione pliki: `index.html`'s `:root`/`:root[data-theme="dark"]` bloki + `.card{border-radius}` + `<meta name="theme-color">` (web), `android/logic/.../AppThemes.kt`'s `"teal"`/`"dark"` wpisy + `AppThemesTest.kt`'s zaktualizowane asercje hex (Kotlin) — pozostałych 9 motywów NIE dotknięto po żadnej stronie. `versions/v71/`, Service Worker `v45→v46`. `./gradlew :logic:test :app:assembleDebug` przechodzi. Ręcznie zweryfikowane na emulatorze 2026-08-10: świeży ekran Przepisy pokazał ciemniejszy leśny zielony nagłówek i mocniej zaokrąglone karty; przełączenie na motyw „Ciemny” w Ustawieniach → Wygląd natychmiast przemalowało tło/karty na prawie czarne, NAGŁÓWEK ZOSTAŁ TEN SAM ZIELONY (kluczowe kryterium referencji), aktywna pigułka „Ciemny” i przycisk „Sprawdź aktualizację” poprawnie pokazały musztardowy/zielony akcent na czarnym tle. Zero crashy.

## Jak to utrzymywać

1. Każda nowa funkcja dodana do `index.html` (wersja web) dostaje odpowiadający wpis/aktualizację tutaj.
2. Jeśli funkcja zostanie od razu przeniesiona też do Kotlina — status ⏳ (do potwierdzenia wizualnie/manualnie w Android Studio lub na emulatorze; od 2026-08-09 lokalna sesja Claude Code na maszynie użytkownika potrafi realnie skompilować i uruchomić testy `./gradlew :app:assembleDebug` / `./gradlew test`, więc błędy kompilacji łapiemy od razu tutaj — ⏳ oznacza już tylko brak weryfikacji UI/UX/emulatora, nie niepewność co do kompilowalności).
3. Jeśli sprawdzisz coś w Android Studio i działa — zmień ⏳ na ✅ (albo daj mi znać, zrobię to sam).
4. Jeśli sprawdzisz i NIE działa — zostaw jako ⏳ i opisz błąd, poprawię.
5. Jeśli nowa logika biznesowa da się wydzielić bez Androida (jak `PantryOperations`/`ShoppingOperations`/`RecipeBrowsing` w `android/logic/`) — dostaje testy JUnit w tej samej turze, żeby faktycznie zweryfikowane pozostawało zweryfikowane przy kolejnych zmianach (regresja), zamiast znów zgadywać. Patrz `android/README.md`, sekcja "Testy automatyczne".
6. **Zmiana w web i Kotlinie idzie równolegle, w tej samej turze pracy** (na życzenie użytkownika, 2026-08-10) — jeśli coś zmienia się w `index.html`, ta sama zmiana ma iść od razu do `android/`, chyba że jest do tego świadomy powód (patrz p. 2). Kolumna „Status w web" istnieje właśnie po to, żeby dało się na pierwszy rzut oka zobaczyć, gdzie obie wersje się rozjeżdżają — pilnuj żeby ona i realny stan `Functional requirements/FR-N.md` (`**Status:**` w nagłówku) się zgadzały, zwłaszcza jeśli użytkownik kiedyś świadomie wyłączy jakąś funkcję tylko w jednej z wersji (wtedy status w tej kolumnie zmienia się na ❌, nie kasuje się wiersza).

