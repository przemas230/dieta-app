# Add project specific ProGuard rules here.
# Empty for now — minifyEnabled is off for the release build type until the
# app is closer to a real release build (see android/README.md, step 6 of
# the publishing guide).
