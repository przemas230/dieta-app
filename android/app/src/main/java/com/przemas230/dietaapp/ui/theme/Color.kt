package com.przemas230.dietaapp.ui.theme

import androidx.compose.ui.graphics.Color

// Matches the web app's theme_color/background_color (manifest.json) so the
// two versions feel like the same product.
val TealPrimary = Color(0xFF1F6B5C)
val TealDark = Color(0xFF163F37)
val CreamBackground = Color(0xFFFAF7F0)
